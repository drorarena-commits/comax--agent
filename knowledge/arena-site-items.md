# הקמת מוצרים באתר ארנה ישראל — המבנה, השערים והמלכודות

עדכון אחרון: **12/09/2026**. נמדד חי מול `arenaisrael.co.il` דרך ה-MCP של
וורדפרס (קריאה בלבד) — 401 מוצרים מפורסמים, WooCommerce על תבנית **Flatsome**.

**הסוכן שמחזיק את זה: [`arena-item-setup`](../.claude/agents/arena-item-setup.md).**
המסמך הזה הוא הידע; הסוכן הוא מי שקורא אותו לפני שהוא כותב לאתר.

⚠️ **מצב: המבנה מופו ואומת בקריאה · היצירה עצמה עדיין לא הורצה.** אין להקים
מוצר ראשון בלי שדרור צופה בהרצה. ראו "מה עדיין לא אומת" בסוף.

---

## היחידה שמקימים

מוצר ארנה טיפוסי באתר הוא **מוצר משתנה (variable)**:

```
product (הורה)            ← דגם.  _sku = קוד הדגם (למשל 004766)
└── product_variation     ← צבע × מידה.  _sku = הברקוד (EAN, 13 ספרות)
```

**זה המקום שבו הכי קל לטעות:** ה-`_sku` של ההורה **אינו ברקוד** אלא קוד הדגם,
וה-`_sku` של הווריאציה **הוא** הברקוד. רק הווריאציה היא זו שהתוסף מצליב מול
קומקס, ולכן רק היא חייבת להיות זהה בדיוק למק"ט בקומקס
(`site-comax-sku-must-match`).

---

## מפת השדות — נמדדה על מוצרים חיים

### ההורה (`post_type: product`)

| שדה | ערך | מקור/דוגמה |
|---|---|---|
| `title` | שם עברי + שם הדגם באנגלית | `בגד ים שלם לנשים WOMEN'S TEAM SWIMSUIT CHALLENGE SOLID` |
| `terms.product_type` | `variable` (או `simple` לפריט בלי צבע/מידה) | |
| `terms.product_cat` | קטגוריות — **תמיד גם קטגוריית קהל** (נשים/גברים/ילדים ונוער/יוניסקס) **וגם קטגוריית מוצר** | `["בגדי ים","בגדי ים שלמים","נשים"]` |
| `terms.pa_color` | כל הצבעים של הדגם | `803-NEON BLUE-TEAM ORANGE` |
| `terms.pa_size` | כל המידות | `32,34,36,38,40,42` |
| `meta._sku` · `meta._parent_sku` | **קוד הדגם**, זהה בשניהם | `004766` |
| `meta._product_attributes` | ראו למטה — **חובה, אחרת אין ווריאציות** | |
| `meta._default_attributes` | הצבע והמידה שנפתחים כברירת מחדל, **בסלאגים** | `{"pa_color":"803-neon-blue-team-orange","pa_size":"32"}` |
| `meta._price` | המחיר שמוצג בארכיון | `199` |
| `meta._manage_stock` | `no` בהורה — המלאי יושב בווריאציות | |
| `meta._thumbnail_id` · `_product_image_gallery` | תמונה ראשית + גלריה (מזהי מדיה, פסיקים) | `77117` · `78216,78213,78215` |
| `meta.additional_description` | **הרכב הבד/החומר** — שדה ACF שמוצג בכרטיס | `הרכב בד: 100% פוליאסטר` |
| `meta._additional_description` | `field_638896180e1c6` — **מפתח ה-ACF, חייב להיכתב יחד איתו** | |
| `excerpt` | התיאור הקצר — שתי פסקאות HTML | |

**`_product_attributes`** — המבנה המדויק שנמדד, ובלעדיו WooCommerce לא יידע
שהמוצר משתנה:

```json
{"pa_color":{"name":"pa_color","value":"","position":0,"is_visible":1,"is_variation":1,"is_taxonomy":1},
 "pa_size": {"name":"pa_size", "value":"","position":1,"is_visible":1,"is_variation":1,"is_taxonomy":1}}
```

### הווריאציה (`post_type: product_variation`, `parent` = מזהה ההורה)

| שדה | ערך |
|---|---|
| `meta.attribute_pa_color` · `attribute_pa_size` | **הסלאג של המונח**, לא שמו — `906-very-fuchsia-team-green` · `36` |
| `meta._sku` | **הברקוד. זה המפתח לקומקס** — `3468337730286` |
| `meta._regular_price` **וגם** `meta._price` | שניהם. `_price` לבדו לא נשמר, `_regular_price` לבדו לא מוצג |
| `meta._manage_stock` = `yes` · `_stock` · `_stock_status` | המלאי הראשוני; מכאן והלאה הסנכרון מקומקס מעדכן |
| `meta._tax_class` | `parent` |
| `meta._thumbnail_id` | **תמונת הצבע** |
| `meta._ux_additional_variation_images` | **שאר תמונות הצבע**, מזהי מדיה מופרדים בפסיקים |

---

## התמונות — הכלל שמחבר את הסקיל לאתר

**התמונות באתר הן ברמת הצבע, לא ברמת המידה.** כל הווריאציות של אותו צבע
נושאות **בדיוק את אותם** `_thumbnail_id` ו-`_ux_additional_variation_images`.
נמדד על 004766: שש מידות בצבע 906, כולן `_thumbnail_id: 77113` ו-
`_ux_additional_variation_images: 77114,77115,77116`.

זה בדיוק מה שהסקיל **`arena-image-sourcing`** מייצר: 4 תמונות לכל
מק"ט-צבע. אחת נעשית `_thumbnail_id` והשאר `_ux_additional_variation_images`,
ואותה רביעייה נכתבת לכל המידות של הצבע.

`_ux_additional_variation_images` הוא תוסף של Flatsome. **שם השדה הזה אינו
מנחש — הוא נמדד**, ואסור להחליף אותו ב-`_product_image_gallery` ברמת הווריאציה.

---

## שלושת השערים שלפני הכתיבה

1. **הפריט קיים בקומקס, עם אותו מק"ט בדיוק.** תוסף הסנכרון **מפיל הזמנה
   שלמה** שמכילה פריט שלא קיים בקומקס — בשקט, בלי שגיאה
   (`site-comax-sku-must-match`). ההצלבה היא מול קובץ הפריטים ב-`content/`,
   ואם משהו חסר — קודם `items-export` טרי, ואז `comax-items`, ורק אז האתר.
   **ואסור "לנרמל כדי שיתאים"** — `3468333894005-41342` אינו
   `3468333894005`.
2. **הסימון "לא להציג" בוטל בקומקס** במסך נתוני אתר
   (`site-display-flag-must-be-cleared`). זה שלב שאין לו שום סימן חיצוני:
   מוצר יכול להיות מושלם בשני הצדדים ועדיין לא לדבר.
3. **יש תמונות.** מוצר בלי תמונות אינו מוצר — הסקיל רץ **לפני** הכתיבה לאתר,
   לא אחריה.

---

## מלכודות

1. **`_price` ו-`_regular_price` הם שני שדות.** כתיבת אחד בלבד מייצרת מוצר
   שנראה תקין בממשק הניהול ולא מתומחר בחזית.
2. **הסלאג של המונח אינו שמו.** `attribute_pa_color` מקבל
   `803-neon-blue-team-orange`, בזמן שהמונח עצמו נקרא
   `803-NEON BLUE-TEAM ORANGE`. כתיבת השם תיצור ווריאציה שלא תיבחר לעולם.
3. **`_wm_instock_terms_pa_color` / `_pa_size` נוצרים על ידי תוסף** שמסתיר
   צבעים ומידות שאזלו. **לא לכתוב אותם ידנית** — הם ייבנו מעצמם.
4. **צבע או מידה שלא קיימים כמונח — צריך להקים קודם** (`wp_manage_terms`).
   הקצאת מונח שלא קיים נכשלת בשקט, והמוצר נשאר בלי אותו צבע.
5. **`comax_id` שבווריאציות הוא שדה נטוש** — לא לעדכן אותו, לא להסתמך עליו
   (`site-sync-matches-on-sku-only`).
6. **קופסאות מיקס** — משפחת פריטים שבה כמה צבעים באתר חולקים ברקוד פיזי אחד
   (`mix-box-shared-barcode-pattern`). מוצר כזה אינו הקמה רגילה; לעצור ולשאול.

---

## מה עדיין לא אומת

- **יצירת מוצר או ווריאציה בפועל דרך ה-MCP לא הורצה.** כל המפה למעלה נקראה
  ממוצרים קיימים. בפרט לא אומת ש-`wp_create_post` עם `post_type:
  product_variation` + `parent` מייצר ווריאציה שוורדפרס באמת מכירה, ולא
  אומת שהוא מפעיל את `wc_delete_product_transients` — ייתכן שיידרש שמירה
  ידנית אחת במסך המוצר כדי לרענן.
- **סנכרון המלאי מקומקס לאתר** — לא מופה כאן בכלל.
- **מדריך המידות** (`size_guide` / `size_guide_link`, שדות ACF) — נראה בשדות
  ולא נבדק איך ממלאים אותו.

כשמשהו מזה ייבדק חי — לכתוב לכאן ול-`.claude/agents/arena-item-setup.md`
באותו commit (כלל 12).

---

## מה שנמדד בהקמה הראשונה — POWERFIN PRO II FLEX, 13/09/2026

ההקמה הראשונה רצה מקצה לקצה: מוצר `78760`, 18 ווריאציות, 12 תמונות WebP.
**היצירה דרך ה-MCP עובדת** — `wp_create_post` עם `post_type: product_variation`
ו-`parent` מייצר ווריאציה שוורדפרס וווקומרס מכירים.

### 1. 💣 מוצר בלי מלאי מחזיר **אפס ווריאציות** בחזית — וזה תקין

`form.variations_form` נטען, אבל `data-product_variations` הוא מערך ריק, ולכן
**אין בוררי צבע ומידה בדף**. נבהלתי וחשבתי שהיצירה נכשלה.

**ההוכחה שזו התנהגות החנות ולא כשל:** מוצר 9150 (`Powerfin Pro` הישן), שמפורסם
באתר שנים ושכל הווריאציות שלו אזלו, מחזיר **בדיוק אותו `count: 0`**. ווקומרס
מסנן ווריאציות שאינן `variation_is_visible()`, והחנות מגדירה הסתרה של אזל
מהמלאי (מה שמסביר גם את התוסף שכותב `_wm_instock_terms_pa_color`).

⇒ **מוצר שנוצר עם מלאי 0 ייראה "שבור" עד שייכנס מלאי.** אין לרדוף אחרי
"תיקון" של הסנכרון בגלל זה, ואין להסיק ממנו שהווריאציות לא נוצרו. הבדיקה
הנכונה היא ספירת `product_variation` עם `post_parent` — לא החזית.

### 2. העלאת תמונות — `file_upload` דרך wp-admin, לא base64

העלאת 12 קבצים כ-base64 דרך `wp_upload_media` עולה עשרות אלפי טוקנים וקריאה
נפרדת לכל קובץ. הדרך המהירה: לנווט ב-Claude in Chrome ל-
`wp-admin/media-new.php`, לאתר את שדה הקובץ (`find` → `input[type=file]`)
ולהעלות את **כולם בקריאה אחת** עם `file_upload` (עד 10MB לקריאה).
נמדד: 10 קבצים, 125KB, שניות.

⚠️ ההעלאה **אסינכרונית** — הקבצים נרשמים אחד-אחד. לאמת ב-`wp_list_media` עד
שהמספר מתייצב, לא מיד אחרי הקריאה.
⚠️ `file_upload` **לא מגדיר `alt_text`.** להשלים אחריו ב-`wp_update_post` עם
`_wp_attachment_image_alt`.

### 3. שתי מערכות תמונות באתר, לא אחת

| דור | השדות | דוגמה |
|---|---|---|
| ישן (CommerceKit) | `commercekit_image_gallery` · `commercekit_attribute_swatches`, ממופתחים ב-**מזהה מונח הצבע** | 9150 |
| חדש (Flatsome UX) | `_thumbnail_id` + `_ux_additional_variation_images` על הווריאציה | 77054, 78760 |

**מוצרים חדשים נבנים בדור החדש.** אין לערבב, ואין להסיק מהישן.

### 4. פרטים שנמדדו

- וורדפרס מייצר מהאב סדרי גודל `medium` · `thumbnail` ·
  `woocommerce_gallery_thumbnail` — **גם ל-WebP**. אין צורך להכין גדלים.
- שמירת טיוטה במסך המוצר **אינה** מרעננת את רשימת הווריאציות בחזית כשאין מלאי
  (ראו סעיף 1) — היא לא נדרשת אחרי יצירה דרך ה-MCP.
- מונחי `pa_size` של סנפירים באתר הם **`36/37`–`46/47` עם לוכסן**, בעוד שבקומקס
  `36-37` עם מקף. משתמשים במונחים הקיימים באתר; ההצלבה לקומקס היא דרך המק"ט.

### 5. 💣 שינוי מלאי דרך ה-MCP אינו מרענן את מטמון הצבעים והמידות

זהו תיקון והשלמה לסעיף 1, שנמדד באותו יום כשדרור ביקש לשים מלאי 1 לבדיקה.

בהורה יושבים שני שדות של תוסף ההסתרה:
`_wm_instock_terms_pa_color` ו-`_wm_instock_terms_pa_size`. **הם מחזיקים את
רשימת הערכים שיוצגו**, והם **אינם מתעדכנים** כש-`_stock` משתנה דרך
`wp_update_post`.

נמדד, בשלושה שלבים:

| מצב | מה החזית הראה |
|---|---|
| כל 18 עם מלאי 0 | `count: 0` — אין בוררים בכלל |
| כל 18 עם מלאי 1, בלי שמירה בניהול | **`count: 1`** — צבע אחד ומידה אחת בלבד |
| אחרי **עדכון במסך המוצר** | `count: 18` — 3 צבעים, 6 מידות ✅ |

השלב האמצעי הוא המסוכן: הדף נראה **תקין לגמרי** ומאפשר קנייה — רק שהוא מציג
פריט אחד מתוך שמונה־עשר. **אין שום שגיאה.** השדות נשארו על הערך שהיה נכון
בשנייה שבה עודכנה הווריאציה הראשונה.

⇒ **אחרי כל שינוי מלאי או מחיר שנעשה דרך ה-MCP — לפתוח את המוצר ב-
`wp-admin/post.php?post=<id>&action=edit` וללחוץ "עדכון".** זו הפעולה היחידה
שמרעננת. ואז **לאמת בחזית בספירת `data-product_variations`**, לא בעין.

⚠️ **ולחיצה על הכפתור דרך `ref` מ-`find` לא הגישה את הטופס** — נמדד פעמיים,
ו-`modified` של המוצר לא זז. מה שעבד: צילום מסך ולחיצה על **הקואורדינטה** של
הכפתור הכחול בתיבת הפרסום. לוודא שהופיעה הודעת "המוצר עודכן", ולא להסתפק
בכך שהקליק "הצליח".

### 6. 💣💣 שתי מלכודות שהוציאו את המוצר מהקטלוג — בלי שדף המוצר נשבר

דרור פתח את האתר ואמר "אני לא רואה אותם". **דף המוצר עבד מושלם** — 200,
18 ווריאציות, בוררים, כפתור קנייה — אבל המוצר **לא הופיע באף רשימה**: לא
בקטגוריה, לא בחיפוש. שתי סיבות נפרדות, ושתיהן שקטות לחלוטין.

#### א. שיוך קטגוריה **לפי שם** נוחת בענף הלא נכון

באתר יש **שבע** קטגוריות בשם `יוניסקס` — אחת מתחת לכל ענף (משקפות, כובעים,
תיקים, מגבות, הנעלה, ציוד אימונים, ו"יוניסקס בוגרים") — ו**שתיים** בשם `ציוד`
(`equipment` שמתחת לשורש, ו-`ציוד` שמתחת ל-`uni-adult`). כשמעבירים **שם**
ל-`wp_create_post`, וורדפרס בוחר אחת מהן שרירותית. המוצר שויך ל-`ציוד` היתום
(term 821, `count: 0`) במקום ל-`equipment` (780), ולכן נעלם מהקטלוג.

⇒ **לשייך `product_cat` לפי `term_id` בלבד, לעולם לא לפי שם.** הזוג הנכון
לסנפירים: `780` ציוד · `781` ציוד אימונים · `782` יוניסקס.
⇒ ולפני כל הקמה — `wp_manage_terms` עם `search` על **כל** שם קטגוריה שמתכוונים
להשתמש בו, כדי לראות אם הוא חד-משמעי. שם שמופיע פעמיים הוא **עצירה**, לא
בחירה בראשון.

#### ב. `product_visibility: outofstock` — כתיבת מלאי ישירה משאירה את ווקומרס עיוור

`_stock` ו-`_stock_status` שנכתבים ישירות כ-meta **אינם מעדכנים את
`wc_product_meta_lookup`**. כשהמוצר נשמר אחר כך בממשק הניהול, ווקומרס מסנכרן
את ההורה מהטבלה **הישנה**, מסיק "אזל מהמלאי", ומוסיף להורה את מונח הנראות
`outofstock` — והחנות, שמסתירה אזל מהמלאי, **מוציאה אותו מכל רשימה**.

זהו גם התיקון למה שכתוב בסעיף 1: הגורם אינו "מוצר בלי מלאי" אלא **מונח
הנראות**, ומונח הנראות יכול להידבק גם כשכל הווריאציות במלאי.

**התיקון שעבד:** `wp_update_post` על ההורה עם
`meta: {_stock_status: "instock"}` ו-`terms: {"product_visibility": []}`.
אחריו המוצר הופיע מיד בקטגוריה, בתת-קטגוריה ובחיפוש.

⚠️ **והאימות חייב להיות כגולש אנונימי.** כל הבדיקות שלי עד אז נעשו בדפדפן
מחובר כמנהל, ושם המוצר נראה תקין. `curl` בלי עוגיות על **עמוד הקטגוריה**,
ולא רק על דף המוצר, הוא מה שחשף את הפער. **דף מוצר שעובד אינו הוכחה שהמוצר
נמכר** — הלקוח מגיע דרך הרשימה.

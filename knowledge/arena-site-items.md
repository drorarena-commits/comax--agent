# הקמת מוצרים באתר ארנה ישראל — ספר ההקמה

עדכון אחרון: **13/09/2026**. ✅ **מאומת בהרצה חיה מקצה לקצה** — סנפירי
`POWERFIN PRO II FLEX` (מוצר 78760): 18 ווריאציות, 12 תמונות WebP, 3 ריבועי
צבע, תיאור מתורגם מהמקור, ומופיע בקטלוג ובחיפוש.

האתר: `arenaisrael.co.il` — WooCommerce על תבנית **Flatsome**, ~400 מוצרים.
**הסוכן שמחזיק את זה: [`arena-item-setup`](../.claude/agents/arena-item-setup.md).**
המסמך הזה הוא הידע; הסוכן הוא מי שקורא אותו לפני שהוא כותב לאתר.

---

## 1. הזרימה, בסדר הזה בלבד

| # | שלב | מה קורה | עוצר? |
|---|---|---|---|
| 0 | **התיאורים** | מקובץ ה-Product Matrix של ארנה, לפי עמודת `Style` | |
| 1 | הרשימה | מק"ט-צבע לכל פריט — **מקובץ הפריטים של קומקס** | |
| 2 | **שער קומקס** | כל ברקוד מוצלב מול `content/פריטים-מלא-*.csv` | **חוסם** |
| 3 | **שער "לא להציג"** | הדגל בוטל בקומקס לכל הפריטים | **חוסם** |
| 4 | התמונות | סקיל `arena-image-sourcing` — PIM → קבצים מקומיים | |
| 5 | **המרה ל-WebP** | חובה — אין באתר תוסף שממיר בשרת | |
| 6 | תצוגה מקדימה | התמונות והטבלה לדרור, **וממתינים** | **חוסם** |
| 7 | מדיה | `file_upload` ל-`wp-admin/media-new.php`, ואז `alt_text` | |
| 8 | מונחים | `pa_color` / `pa_size` / `product_cat` — **לפי מזהה** | |
| 9 | הורה | `wp_create_post` · `post_type: product` · `status: draft` | |
| 10 | ווריאציות | `product_variation` לכל צבע × מידה | |
| 11 | **ריבועי צבע** | `tools/site-swatch.js` → העלאה → `ux_image` בכל מונח | |
| 12 | נראות | לוודא שאין `product_visibility: outofstock` על ההורה | |
| 13 | **SEO** | ביטוי מפתח · תיאור מטא · קטגוריה ראשית · `gtin13` לכל ווריאציה | |
| 14 | אימות | ספירת `post_parent` + הצלבה דו-כיוונית + **בדיקה כגולש** | |
| 15 | פרסום | `status: publish` — רק אחרי אישור דרור | **חוסם** |

⛔ **לעולם לא לדלג משלב 1 לשלב 9.** שערים 2 ו-3 הם מה שמונע את הבאג היקר
ביותר באתר: הזמנה שמכילה פריט שלא קיים בקומקס **אינה משודרת בכלל**, בשקט,
והמלאי לא יורד.

---

## 2. מי קובע מה — שני מקורות, ולא להחליף ביניהם

| המקור | מה הוא קובע | מה הוא **לא** קובע |
|---|---|---|
| **קובץ הפריטים של קומקס** | **אילו פריטים וצבעים מוקמים** | תיאורים, מפרט |
| **Product Matrix של ארנה** | תיאורים, מאפיינים, הרכב חומר, מידות, EAN | מה מקימים |

**הכיוון הוא קומקס ⇒ אתר, תמיד.** המטריצה **מעשירה** את מה שכבר נבחר; היא
לא בוחרת. זו הכרעת דרור, והיא לא נתונה לשיקול דעת:

> "תמיד בקובץ המוצרים יכולים להיות וריאציות/צבעים שקיימים ואנחנו לא הזמנו
>  אותם לארץ. ההקמה תהיה תמיד רק לפי מה שיש בארץ בקובץ הקומקס. ובהמשך, אם
>  יגיעו עוד צבעים, אז נוסיף לדגם הקיים."

נמדד: ל-`011938` יש במטריצה **ארבעה** צבעים (100 · 200 · **300
SHIMMERED_PINK** · 400) ובקומקס **שלושה**. ורוד לא הוקם — וזה לא פער לתקן
אלא סחורה שלא הוזמנה.

⇒ **וכשיגיעו צבעים נוספים: מוסיפים לדגם הקיים** — ווריאציות ומונח `pa_color`
חדשים תחת אותו הורה, **לא מוצר משתנה שני לאותו דגם.**

### קובץ ה-Product Matrix

למשל `data/exports/FW26 Global_Matrix_D_En_Global.data.xlsx` — 7,523 שורות ×
49 עמודות, שורה לכל מק"ט-צבע-מידה. נקרא עם
`node tools/xlsx.js <קובץ> <תיקייה>`, ומצליבים לפי **`Style`** (מספר הדגם,
זהה לדגם בקומקס).

| עמודה | לאן |
|---|---|
| `Extended Description` | התיאור הארוך — **לתרגם לעברית** |
| `Product Feature 1–5` | התקציר ורשימת המאפיינים |
| `Style Composition` | `additional_description` — הרכב החומר |
| `Name for the Internet` | שם המוצר באנגלית |
| `Colorway` + `Colorway Description` | קוד ושם הצבע, למונחי `pa_color` |
| `Size` · `EAN` | מידות ומק"טים — להצלבה מול קומקס |
| `RSP AINT EUR (€)` | מחיר צרכן מומלץ באירו |

**התיאור זהה לכל צבעי הדגם** — נמדד: 24 שורות ל-011938, גרסת תיאור אחת.

⛔ **ומה שלא נמצא במקור — לא ממציאים בשקט.** מותר לכתוב מההבנה שלי כשאין
מקור, אבל **חובה להצהיר על כך במפורש** באותו דיווח, ולפרט אם חסר התיאור
הקצר, הארוך או המפרט הטכני. ⛔ **והעתקה מדגם "דומה" היא המצאה** — דגם חדש
אינו יורש מפרט מדגם קודם, גם כשבמקרה הוא זהה.

---

## 3. שלושת השערים שלפני הכתיבה

1. **הפריט קיים בקומקס עם אותו מק"ט בדיוק** (`site-comax-sku-must-match`).
   ההצלבה מול `content/`; חסר ⇒ `items-export` טרי ⇒ ואם עדיין חסר,
   `comax-items`, **ולא ממשיכים לאתר**.
   ⛔ אין לנרמל, לחתוך סיומת או לרפד כדי ש"יתאים".
   ⚠️ ברקוד שאינו בייצוא אינו הוכחה שהפריט חסר — ייתכן שהוא ברקוד נוסף על
   פריט קיים.
2. **הסימון "לא להציג" בוטל בקומקס** (`site-display-flag-must-be-cleared`) —
   `a84` → נוספים → נתוני אתר. **שלב שאין לו שום סימן חיצוני.**
3. **יש תמונות.** מוצר בלי תמונות אינו מוצר. הסקיל רץ **לפני** הכתיבה.

### ביטול הדגל — הזרימה שאומתה חי (13/09/2026)

```
node tools/site-data-file.js data/exports/נתוני-אתר-<דגם>-<תאריך>.xlsx <ברקוד> ...
npm run items-import -- --json '{"file":"<אותו קובץ>","impType":"3"}'            # יבש
npm run items-import -- --json '{"file":"<אותו קובץ>","impType":"3"}' --confirm  # קליטה
npm run run -- items-export        # ואז grep על העמודה "נתוני אתר - לא להציג"
```

נמדד על 18 פריטי `POWERFIN PRO II FLEX`: **18 תקין · 0 לא תקין**, והעמודה
עברה מ-`1` ל**ריק** בכל 18. **ריק ו-0 שקולים** בשדה הזה.
⚠️ `master-gate ⚠ סדר הכותרות אינו IMPORT_HEADERS` הוא **תקין** בסוג 3 —
שער המאסטר בנוי לקובץ ההקמה (סוג 0), לא לקובץ בן ארבע העמודות.

### ⚠️ הדגל והפרסום הם שני מתגים נפרדים

הם נראים כמו אותה שאלה — "שהמוצר יופיע" — ואינם:

| המתג | איפה | מה הוא שולט |
|---|---|---|
| "לא להציג" | **קומקס** | האם הפריט **מסונכרן** בכלל |
| `status` | **האתר** | האם הגולש **רואה** אותו |

**מוצר שהוקם לפני שהסחורה הגיעה נשאר `draft` באתר בזמן שהדגל בקומקס כבר
מבוטל.** כך הסנכרון מתחיל לעבוד ברקע והמוצר יוצא לאוויר רק כשהמלאי בפנים —
זה מה שדרור ביקש על הסנפירים, שמגיעים בעוד כשלושה שבועות.
⛔ להשאיר את הדגל `1` "עד שיגיע המלאי" הוא לא זהיר אלא הפוך: המלאי יגיע
ולא ישודר, בלי שום סימן.

---

## 4. היחידה שמקימים

```
product (הורה)            ← דגם.  _sku = קוד הדגם (למשל 011938)
└── product_variation     ← צבע × מידה.  _sku = הברקוד (EAN, 13 ספרות)
```

💣 **ל-`_sku` שתי משמעויות באותו מוצר.** בהורה הוא **קוד הדגם**, בווריאציה
הוא **הברקוד**. רק הווריאציה מוצלבת מול קומקס, ולכן רק היא חייבת להיות זהה
בדיוק למק"ט שם. בלבול ביניהם מייצר מוצר שנראה תקין ושאף הזמנה שלו לא תשודר.

---

## 5. מפת השדות — נמדדה על מוצרים חיים

### ההורה (`post_type: product`)

| שדה | ערך |
|---|---|
| `title` | שם עברי + שם הדגם באנגלית |
| `terms.product_type` | `variable` (או `simple` לפריט בלי צבע/מידה) |
| `terms.product_cat` | **לפי `term_id`** — ראו פרק 8 |
| `terms.pa_color` · `pa_size` | כל הצבעים וכל המידות |
| `terms.pa_purpose` · `pa_environment` | לא-ווריאציה; למשל `אימונים` · `בריכה` |
| `meta._sku` · `_parent_sku` | **קוד הדגם**, זהה בשניהם |
| `meta._product_attributes` | ראו למטה — **חובה, אחרת אין ווריאציות** |
| `meta._default_attributes` | ברירת המחדל, **בסלאגים** |
| `meta._price` | המחיר שמוצג בארכיון |
| `meta._manage_stock` | `no` — המלאי יושב בווריאציות |
| `meta._thumbnail_id` · `_product_image_gallery` | ראשית + גלריה (מזהים, פסיקים) |
| `meta.additional_description` | הרכב החומר — שדה ACF שמוצג בכרטיס |
| `meta._additional_description` | `field_638896180e1c6` — **מפתח ה-ACF, חובה יחד איתו** |
| `excerpt` | התיאור הקצר — שתי פסקאות HTML |

```json
"_product_attributes": {
 "pa_color":{"name":"pa_color","value":"","position":0,"is_visible":1,"is_variation":1,"is_taxonomy":1},
 "pa_size": {"name":"pa_size", "value":"","position":1,"is_visible":1,"is_variation":1,"is_taxonomy":1}}
```

### הווריאציה (`post_type: product_variation`, `parent` = מזהה ההורה)

| שדה | ערך |
|---|---|
| `meta.attribute_pa_color` · `attribute_pa_size` | **הסלאג**, לא השם |
| `meta._sku` | **הברקוד. זה המפתח לקומקס** |
| `meta._regular_price` **וגם** `_price` | שניהם |
| `meta._manage_stock: yes` · `_stock` · `_stock_status` | המלאי הראשוני |
| `meta._tax_class` | `parent` |
| `meta._thumbnail_id` | תמונת הצבע |
| `meta._ux_additional_variation_images` | שאר תמונות הצבע, בפסיקים |

---

## 6. התמונות

**התמונות באתר הן ברמת הצבע, לא ברמת המידה.** כל הווריאציות של אותו צבע
נושאות בדיוק את אותם `_thumbnail_id` ו-`_ux_additional_variation_images`.

| התמונה מהסקיל | לאן |
|---|---|
| `...-001` | `_thumbnail_id` של **כל** ווריאציות הצבע |
| `...-002/3/4` | `_ux_additional_variation_images` של אותן ווריאציות |
| תמונות הצבע הראשי | `_thumbnail_id` + `_product_image_gallery` של ההורה |

**המרה ל-WebP היא חובה** — 618 קבצי WebP באתר, והמוצרים החדשים הועלו עם
סיומת `.webp`, כלומר ההמרה נעשית לפני ההעלאה ואין תוסף שממיר בשרת. נמדד:
278KB ⇒ 136KB, חיסכון 51%, איכות 85.

**ההעלאה: `file_upload` ל-`wp-admin/media-new.php`, כל הקבצים בקריאה אחת**
(עד 10MB). base64 קובץ-קובץ עולה עשרות אלפי טוקנים ומיותר.
⚠️ ההעלאה **אסינכרונית** — לאמת ב-`wp_list_media` עד שהמספר מתייצב.
⚠️ `file_upload` **לא מגדיר `alt_text`** — להשלים ב-`wp_update_post` עם
`_wp_attachment_image_alt`.

⚠️ **ובאתר שתי מערכות תמונות במקביל:** הישנה היא CommerceKit
(`commercekit_image_gallery`, ממופתחת במזהה מונח הצבע), החדשה היא
`_ux_additional_variation_images`. **מוצר חדש נבנה בחדשה בלבד.**

---

## 7. ריבועי בחירת הצבע — האתר לא מייצר אותם, אנחנו כן

> "האתר לא מייצר אותם לבד. אתה צריך לייצר אותם בהתאם לגודל הקבוע שלהם,
>  כריבוע 'דגימה' של הצבע של המוצר עצמו." — דרור

| | |
|---|---|
| הגודל | **50×50 WebP** — נמדד על ריבוע קיים |
| התוכן | **חיתוך אמיתי מצילום המוצר**, לא ריבוע צבע מלאכותי |
| שם הקובץ | `<דגם><קוד-צבע>_thumb-<שם הצבע במקפים>.webp` |
| איפה נשמר | **term meta `ux_image`** על מונח ה-`pa_color` |

**`ux_image` הוא השדה, וזה מה שאי אפשר לנחש.** הוא שדה נסתר במסך עריכת
המונח (`wp-admin/term.php?taxonomy=pa_color&tag_ID=<id>&post_type=product`),
והדרך שעבדה היא להציב בו את מזהה המדיה ולשלוח את הטופס `#edittag`.
**ה-MCP של וורדפרס אינו כותב מטא של מונח** — השלב הזה חייב לעבור בדפדפן.

הכלי: **[`tools/site-swatch.js`](../tools/site-swatch.js)** — סורק את צילום
המוצר ומאתר את הריבוע שכולו מוצר (בלי הרקע הלבן) ושהשונות בו הנמוכה ביותר,
כלומר משטח בד אחיד בלי לוגו, תפר או צל.

**אותו ריבוע משרת גם את דף המוצר וגם את הקטלוג** (`swatches-in-loop`).
⚠️ מוצר בלי ריבועים מציג ריבועי "ממלא מקום" של ווקומרס ו**נראה שבור ללקוח**
גם כשכל השאר תקין.

---

## 8. הקטגוריות — לפי מזהה בלבד

💣💣 **באתר שבע קטגוריות בשם `יוניסקס`** — אחת מתחת לכל ענף — **ושתיים בשם
`ציוד`**. כשמעבירים **שם** ל-`wp_create_post`, וורדפרס בוחר אחת מהן
שרירותית. נמדד: המוצר שויך ל-`ציוד` היתום (term 821, `count: 0`) במקום
ל-`equipment` (780), ונעלם מכל הרשימות בזמן שדף המוצר עבד מושלם.

⇒ **לשייך `product_cat` לפי `term_id` בלבד.** לסנפירים: `780` ציוד ·
`781` ציוד אימונים · `782` יוניסקס.
⇒ **ולפני כל הקמה** — `wp_manage_terms` עם `search` על כל שם קטגוריה
שמתכוונים להשתמש בו. **שם שמופיע פעמיים הוא עצירה, לא בחירה בראשון.**

**לכל מוצר קטגוריית קהל וגם קטגוריית מוצר.** מוצר בלי קטגוריית קהל נעלם
מהסינון בחנות.

---

## 9. המלכודות

1. 💣 **`_price` ו-`_regular_price` הם שני שדות.** אחד לבדו נראה תקין
   בניהול ולא מתומחר בחזית.
2. 💣 **`attribute_pa_color` מקבל סלאג, לא שם.** שם = ווריאציה שלא תיבחר
   לעולם.
3. 💣 **`_product_attributes` בהורה הוא מה שהופך מוצר למשתנה.** בלעדיו
   הווריאציות קיימות במסד ולא קיימות בחנות.
4. 💣 **כתיבת `_stock` ישירות מייצרת `product_visibility: outofstock`.**
   ה-meta לא מעדכן את `wc_product_meta_lookup`; ווקומרס מסנכרן את ההורה
   מהטבלה הישנה, מסיק "אזל", והחנות **מוציאה אותו מכל רשימה** בזמן שדף
   המוצר ממשיך לעבוד.
   **התיקון:** `_stock_status: "instock"` + `terms: {"product_visibility": []}`.
5. 💣 **שינוי מלאי דרך ה-MCP אינו מרענן את `_wm_instock_terms_pa_color/size`.**
   השדות האלה בהורה מחזיקים את רשימת הערכים שיוצגו. נמדד בשלושה שלבים:

   | מצב | מה החזית הראתה |
   |---|---|
   | 18 ווריאציות עם מלאי 0 | `count: 0` — אין בוררים |
   | 18 עם מלאי 1, בלי שמירה בניהול | **`count: 1`** — צבע אחד ומידה אחת |
   | אחרי **"עדכון" במסך המוצר** | `count: 18` ✅ |

   השלב האמצעי הוא המסוכן: דף שנראה תקין לגמרי ומוכר פריט אחד מתוך 18.
   ⚠️ **והקליק על כפתור העדכון חייב להיות לפי קואורדינטה** — `ref` מ-`find`
   לא הגיש את הטופס, פעמיים. לוודא שהופיעה הודעת "המוצר עודכן".
6. **`_wm_instock_terms_*` נוצר על ידי תוסף** — לא לכתוב ידנית.
7. **`comax_id` בווריאציות הוא שדה נטוש** — לא לעדכן
   (`site-sync-matches-on-sku-only`).
8. **קופסת מיקס אינה הקמה רגילה** — כמה צבעים שחולקים ברקוד פיזי אחד
   (`mix-box-shared-barcode-pattern`). לעצור ולשאול.
9. **וורדפרס מייצר מהאב את כל הגדלים** (`medium` · `thumbnail` ·
   `woocommerce_gallery_thumbnail`) — גם ל-WebP. אין צורך להכין גדלים.

---

## 9ב. ה-SEO — ארבעה שדות, וכולם ממקור קיים

התוסף הפעיל הוא **Yoast SEO Premium 27.5** יחד עם **Yoast SEO: WooCommerce
16.8**. בלי המילוי הזה Yoast נופל לתבנית ברירת המחדל, וגוגל מקבל את שם
המוצר פעמיים: נמדד על מגבת חיה — `"SMART PLUS GYM TOWEL BLUE-RED - Arena
Israel"` כתיאור המטא כולו.

| השדה | המפתח | מהיכן |
|---|---|---|
| ביטוי מפתח | `_yoast_wpseo_focuskw` | שם המוצר בעברית + `Arena` + שם הדגם |
| תיאור מטא | `_yoast_wpseo_metadesc` | עד ~155 תווים, מתומצת מהתיאור הארוך |
| קטגוריה ראשית | `_yoast_wpseo_primary_product_cat` | **מזהה**, כמו כל קטגוריה |
| `gtin13` | ראה למטה | ה-EAN — כבר ה-`_sku` של הווריאציה |

**התבנית של האתר עצמו** היא מוצר 78181 (אטמי אוזניים), שהוקם ידנית בידי
הצוות: `focuskw` = `"אטמי אוזניים לשחייה Arena Earplug"`, ו-`primary_cat`
= `781` — **לא** הקטגוריה העמוקה ביותר אלא קטגוריית המוצר. לשמור על אותה
מוסכמה.

### 💣 `gtin13` יושב על הווריאציה, ובמפתח שאי אפשר לנחש

| הרמה | המפתח |
|---|---|
| הורה | `wpseo_global_identifier_values` → `{gtin8,gtin12,gtin13,gtin14,isbn,mpn}` |
| **ווריאציה** | **`wpseo_variation_global_identifiers_values`** → `{gtin8,gtin12,gtin13,gtin14,mpn}` |

נמדד 13/09/2026 ממסך המוצר עצמו: שדות הטופס הם
`yoast_seo[gtin13]` בהורה ו-`yoast_seo_variation[<מזהה>][gtin13]`
בווריאציה. **ההורה של מוצר משתנה אינו מקבל `gtin13`** — אין לו EAN אחד.

⚠️ **הניחוש הסביר כאן שגוי.** `wpseo_variation_identifiers` על ההורה נשמע
נכון ואינו קיים. מדדתי במסך לפני שכתבתי — וזה מה שמנע כתיבה למפתח מת,
שהייתה נראית כמו הצלחה בכל בדיקה שאינה קריאת הפלט המרונדר.

ההוכחה היא ה-JSON-LD, לא המטא: 18 ערכי `gtin13` בגרף, עם `ProductGroup` ·
`Product` · `Offer`. בלעדיהם אין תוצאת מוצר עשירה בגוגל ואין פיד תקין
ל-Merchant (`Product Catalog Feed by PixelYourSite` פעיל באתר).

### מה המטריצה נותנת — ומה לא

| העמודה | השימוש |
|---|---|
| `Keywords` | חומר גלם לביטוי המפתח — **אנגלית, לתרגום** |
| `Name for the Internet` | שם שארנה ניסחה לשימוש באינטרנט |
| `EAN` | ה-`gtin13`, מק"ט-צבע-מידה |

⛔ **`Keywords` אינו ביטוי המפתח.** הוא גלובלי ובאנגלית, והשאלה מה ישראלים
מקלידים — `"סנפירי אימון"` מול `"זומרס"` — אינה נמצאת בקובץ.

⚠️ **`_yoast_wpseo_linkdex` (ציון ה-SEO של Yoast) לא מתעדכן מכתיבת MCP.**
הוא מחושב רק כשנפתח העורך ונשמר. זו תצוגה פנימית ואינה משפיעה על הפלט,
אבל אין להציג "ציון 0" כממצא.

---

## 10. האימות — כגולש, לא כמנהל

⚠️ **זה הלקח שעלה הכי ביוקר.** כל הבדיקות נעשו בדפדפן מחובר כמנהל, ושם המוצר
נראה תקין — בזמן שהוא לא הופיע באף רשימה. מה שחשף את הפער היה `curl` בלי
עוגיות **על עמוד הקטגוריה**.

**דף מוצר שעובד אינו הוכחה שהמוצר נמכר** — הלקוח מגיע דרך הרשימה.

רשימת האימות:

1. `wp_query_posts` עם `post_parent` — **מספר הווריאציות**.
2. הצלבת מק"טים מול קומקס **בשני הכיוונים**, בלי כפילויות.
3. `curl` אנונימי על **דף המוצר** — 200, ו-`data-product_variations` מלא.
4. `curl` אנונימי על **עמוד הקטגוריה, תת-הקטגוריה והחיפוש**.
5. **מראה בורר הצבע** — שהריבועים אינם `woocommerce-placeholder`.
6. **ה-SEO בפלט המרונדר** — `<meta name="description">` הוא הטקסט שכתבנו
   ולא שם המוצר, וב-JSON-LD יש `Product` עם `gtin13` לכל ווריאציה.
   ⚠️ במוצר בטיוטה זה נבדק דרך כתובת ה-`preview` בדפדפן מחובר, כי `curl`
   אנונימי יחזיר 404 בצדק.

---

## 11. מה עדיין לא אומת

- **סנכרון המלאי מקומקס לאתר** — לא מופה. ✅ הצד של קומקס נפתח: הדגל בוטל
  ב-13/09/2026 על 18 פריטי הסנפירים, והם ממתינים לסחורה. **זו ההזדמנות
  למדוד** — כשהמלאי יגיע, לבדוק אם הוא זרם לאתר לבדו.
  ⚠️ שווה לבדוק אם התוסף מרענן את
  `_wm_instock_terms_*`; אם לא, זו אותה תקלה שקטה של מלכודת 5 על מוצרים
  אמיתיים.
- **מדריך המידות** (`size_guide` / `size_guide_link`, שדות ACF) — נראה
  בשדות ולא נבדק איך ממלאים.
- **הורדת המקור המלא מה-PIM** — כפתור ה-Download מייצר קובץ אבל הוא לא נחת
  על הדיסק מהדפדפן המונחה. התמונות שבשימוש הן תצוגת ה-PIM בת 628×768.

כשמשהו מזה ייבדק חי — לכתוב לכאן ול-`.claude/agents/arena-item-setup.md`
באותו commit (כלל 12).

---

## 12. הוספת ואריאציה לדגם שכבר קיים באתר — מסלול נפרד

נמדד מקצה לקצה ב-13/09/2026 על `004907` (מכנס ברמודה יוניסקס, מוצר 13405):
נוסף צבע `700-NAVY` עם מידות M ו-L, 252 יחידות, והמוצר חי בחזית.

זה **לא** תת-מקרה של פרק 1. שם בונים הורה חדש; כאן נכנסים למוצר קיים,
שנבנה לפני שנים בקונבנציות אחרות, ורוב המלכודות נובעות בדיוק מזה.

### הכלל של דרור — מה נדרש תלוי בצבע, לא בכמות

| המצב | מה עושים |
|---|---|
| **מידה חדשה, צבע קיים** | ואריאציה בלבד. `_thumbnail_id` ו-`_ux_additional_variation_images` **מועתקים כמו שהם** מוואריאציה קיימת של אותו צבע. אין PIM ואין ריבוע. |
| **צבע חדש** | PIM ⇒ WebP ⇒ מדיה ⇒ גלריה ⇒ ריבוע. |

והסיווג נקרא **ממונחי ה-`pa_color` של ההורה באתר**, לא מהדגל בקומקס. הדגל
אומר "מוכן לסנכרן", לא "הצבע קיים באתר".

### 💣 ההורה מאותר לפי ברקוד, לא לפי מק"ט הדגם

`_sku` של ההורה אמור להיות קוד הדגם, ובמוצרים ותיקים הוא פשוט אחר: ב-13405
הוא **`4907`** בלי אפסים מובילים, בזמן ש-`_parent_sku` הוא `004907`. חיפוש
32 דגמים לפי מק"ט-דגם החזיר 9; חיפוש לפי **ברקוד של ואריאציה קיימת** ומשם
`post_parent` החזיר 29. ⇒ תמיד דרך הברקוד.

### 💣💣 `ux_image` הוא מטא של מונח — כלומר **משותף לכל המוצרים**

`pa_color` הם מונחים גלובליים. `700-NAVY` הוא term 582 ומשמש **7 מוצרים**,
ול-`ux_image` שלו כבר הוצב `66280` (`004896700_thumb.png`) מדגם אחר.
⛔ **כתיבת ריבוע חדש למונח קיים דורסת את הריבוע של כל שאר המוצרים.**
⇒ לפני ייצור ריבוע — לבדוק אם למונח כבר יש `ux_image`. אם כן, **לא נוגעים**,
וגם לא צריך לייצר. ריבוע חדש נדרש רק למונח `pa_color` שנוצר עכשיו.
(במקרה הזה יוצר ריבוע מיותר, `78786`, לפני שהבדיקה נעשתה — הוא נשאר במדיה
בלי שימוש.)

### 💣 באתר שתי מערכות תמונות, ובמוצר ותיק צריך למלא את **שתיהן**

13405 מחזיק גם `commercekit_image_gallery` (ממופתח במזהה מונח הצבע) וגם
`_ux_additional_variation_images` על הוואריאציה. הכלל מפרק 6 — "מוצר חדש
נבנה בחדשה בלבד" — נכון להקמה חדשה, **ולא** להרחבת מוצר קיים.
⇒ **קוראים את ההורה ומרחיבים את מה שהוא כבר משתמש בו.** בפועל מולאו:
`commercekit_image_gallery["582"]` · `commercekit_attribute_swatches[6]["582"]`
· `_thumbnail_id` + `_product_image_gallery` + `_ux_additional_variation_images`
על כל ואריאציה.

### 💣💣💣 הכפתור "עדכון" — `.click()` ב-JS עובד, קליק בקואורדינטה לא

זה מתקן את מה שנכתב במלכודת 5 בפרק 9. נמדד שלוש פעמים על אותו מוצר:

| הניסיון | התוצאה |
|---|---|
| קליק לפי **קואורדינטה** מחושבת (אומת ש-`elementFromPoint` מחזיר את `#publish`) | הטופס **לא** הוגש. אין הודעת שמירה, `modified` לא זז |
| קליק לפי **`ref`** מ-`find` | לא הוגש (נמדד בהקמה הקודמת) |
| **`document.querySelector('#publish').click()`** | ✅ הוגש. החזית עברה מ-6 ל-8 ואריאציות |

⚠️ **ולמה זה קריטי:** יצירת ואריאציה דרך ה-MCP כותבת ישירות ל-`wp_posts`
ועוקפת את ה-CRUD של ווקומרס, ולכן `wc_product_meta_lookup` ומטמון הילדים
לא מתעדכנים. **דף המוצר ממשיך להיראות תקין לגמרי ומוכר רק את הצבעים
הישנים.** ההוכחה היחידה היא ספירת `sku` ב-`data-product_variations` בקריאה
**אנונימית** של דף המוצר — לא מסך הניהול, שהראה `total: 8` כשהחזית הראתה 6.

### הסדר שעבד

1. קריאת ההורה — מונחי צבע ומידה קיימים, מחיר שכן, ואיזו מערכת תמונות.
2. PIM: גיליון מגע מהתמונונות ⇒ בחירה ⇒ שליפת כתובות ה-detail ⇒ WebP.
3. העלאה למדיה ב-`file_upload` אחד.
4. `wp_update_post` על ההורה: `_product_attributes` (הוספת מזהה המונח
   לרשימה מופרדת ב-`|`), `terms.pa_color`, גלריית CommerceKit, ריבוע CommerceKit.
5. `wp_create_post` לכל ואריאציה — `attribute_pa_*` **בסלאג**, `_sku` = ברקוד,
   `_regular_price` **וגם** `_price`, `_stock` + `_stock_status: instock`,
   `wpseo_variation_global_identifiers_values.gtin13`.
6. **`#publish` דרך `.click()`**, ואז אימות אנונימי בחזית.

### מה שנשאר פתוח

- `_wm_instock_terms_pa_color` נשאר `"500-black"` גם אחרי שמירה מוצלחת, בזמן
  שהחזית כן מציגה את שני הצבעים. במוצרים בריאים אחרים (13335, 13364) השדה
  הוא **מערך** ולא מחרוזת. ייתכן שהמחרוזת היא צורה ישנה שהתוסף כותב מחדש רק
  בתנאים מסוימים. לא חסם — אבל לבדוק שוב בדגם הבא.

### 💣 העברת `pa_color` **בשם** מחליפה מונח דומה בשקט

נמדד 13/09/2026 על מוצר 76190 (תיק ONE GO). ההורה החזיק את המונח
**`100--BLACK`** (מזהה 1193, סלאג `100-black-2`). עדכון שהעביר את רשימת
הצבעים כשמות — ובתוכה `"100--BLACK"` — החזיר מהשרת **`100-BLACK`**
(מזהה 874), מונח אחר לגמרי. הוואריאציות השחורות הקיימות נשענות על הסלאג
`100-black-2`, כך שהיה נוצר מוצר שמציג בורר צבע שאף ואריאציה לא עונה לו.

⇒ **מעבירים `pa_color` לפי `term_id` בלבד**, בדיוק כמו `product_cat`
(פרק 8). הכלל אינו ייחודי לקטגוריות — הוא נכון לכל טקסונומיה שיש בה שמות
קרובים, ובאתר הזה יש עשרות: `100-BLACK` · `100--BLACK` · `100-BLACK-WHITE`
· `100-BLACK - TEAL`.
⚠️ **ולבדוק את ה-`terms` שחוזרים בתשובה**, לא רק את קוד ההצלחה — זה מה
שחשף את ההחלפה.

### 💣 ריבוע צבע ממוצר עם טרים כהה

`tools/site-swatch.js` בוחר את המשטח האחיד ביותר, ובמוצר עם פאנל שחור זה
הפאנל — לא הצבע. נמדד על `010231-800 ICE`: הריבוע יצא `rgb(28,44,63)`
בזמן שהתיק אפור בהיר. הכלי עודכן עם `MIN_LUM = 100` וסריקה צפופה יותר
(`CROP 90 · STEP 6 · SUB 3`), והתוצאה הייתה `rgb(143,138,137)` עם שונות 45.
⚠️ **ההוכחה היא להסתכל על הריבוע ליד המוצר**, לא ערך ה-RGB לבדו.

### שם הצבע: המטריצה גוברת על שם הפריט בקומקס

`010231-800` נקרא בקומקס **"TEAM BACKPACK 45 ARMY MELANGE"** ובמטריצה
**ICE**. התמונה מה-PIM מראה תיק **אפור בהיר** — המטריצה צודקת ושם הפריט
בקומקס שגוי. ⇒ כששני המקורות נחלקים, **התמונה מכריעה**, והשם בקומקס מדווח
לדרור כתקלת נתונים.

### מתי עוצרים על צבע

`005309-910` (חלוק Zeal Plus Junior, 19 יחידות): אין שם צבע במטריצה, אין
אף נכס ב-PIM תחת הקוד הזה (יש 100/101/200/201/300/301/400/401), ואין צבע
כזה באתר. שלושת המקורות ריקים ⇒ **עצירה**, לא ניחוש ולא העתקה מצבע דומה.

---

## 13. 💣💣💣 מוצר חדש נשאר `outofstock` ונעלם מהחנות — והכפתור "עדכון" לא מתקן

נמדד 13/09/2026 על מוצר **78830** (`004900`, חולצת טי, 25 ואריאציות, 552
יחידות). זו המלכודת היקרה ביותר שנמצאה עד כה, כי **כל שלב נראה תקין**:

| מה נבדק | מה הוחזר |
|---|---|
| דף המוצר, `curl` אנונימי | ✅ 200, **25 ואריאציות** ב-`data_product_variations` |
| ריבועי צבע · `gtin13` · תיאור מטא | ✅ הכול נכון |
| מסך הניהול | ✅ `total: 25`, תצוגת קטלוג `visible` |
| **עמוד הקטגוריה** | ❌ **המוצר לא מופיע** |
| **החיפוש** | ❌ לא מופיע |

השורש, מרשימת המוצרים בניהול: **תצוגה `visible` אבל מצב מלאי `outofstock`
ברמת ההורה.** החנות מוגדרת להסתיר מוצרים שאזלו, ולכן ההורה נופל מכל רשימה
בזמן שדף המוצר שלו עובד מושלם ומוכר את כל 25 הוואריאציות.

**למה זה קורה:** `wp_create_post` כותב ישירות ל-`wp_posts` ועוקף את ה-CRUD
של ווקומרס. ההורה של מוצר משתנה גוזר את מצב המלאי שלו **מהילדים**, והגזירה
הזאת לא רצה. `_stock_status: "instock"` על ההורה **אינו מספיק** — הוא meta,
והחנות שואלת את `wc_product_meta_lookup`.

### ⛔ הכפתור "עדכון" **לא** פותר את זה

`#publish.click()` כן מסנכרן את רשימת הילדים (זה מה שהחזיר את הוואריאציות
לדף המוצר, פרק 12) — אבל **לא** את מצב המלאי הנגזר. הרצתי אותו, ואחריו
ההורה עדיין היה `outofstock`.

### ✅ מה שכן פותר: "שמירת שינויים" בלשונית הוואריאציות

```js
// 1. לפתוח את הלשונית — היא טוענת את הוואריאציות ב-AJAX
document.querySelector('a[href="#variable_product_options"]').click();
// 2. לגעת בשדה כלשהו כדי לשחרר את הכפתור (הוא disabled עד שיש שינוי)
const inp = document.querySelector('#variable_product_options input[name^="variable_stock["]');
inp.value = inp.value;
inp.dispatchEvent(new Event('change', { bubbles: true }));
// 3. לשמור — זה עובר דרך ה-CRUD ומסנכרן את ההורה
document.querySelector('button.save-variation-changes').click();
```

מיד אחרי זה עמוד הקטגוריה והחיפוש החזירו את המוצר.

⚠️ **וההוכחה חייבת להיות עמוד הקטגוריה, לא דף המוצר.** דף מוצר תקין עם
25 ואריאציות פעילות הוא בדיוק מה שראיתי כשהמוצר היה בלתי נמכר לחלוטין.
⚠️ **ובחיפוש — לחפש במונח שבאמת בכותרת.** `?s=Team+Unisex+Solid` החזיר 0
בזמן ש-`?s=T-Shirt` ו-`?s=חולצת טי` החזירו את המוצר; אפס בחיפוש אחד אינו
ראיה.

ℹ️ הוספת ואריאציה למוצר **קיים** (פרק 12) אינה סובלת מזה — ההורה כבר
`instock` בטבלה. הבעיה ייחודית להורה **חדש**.

---

## 14. ההעלאה למדיה — שתי מלכודות שנמדדו ב-13/09/2026

### 💣 `file_upload` דורש **לרענן את הדף ולאתר מחדש את השדה** בין העלאה להעלאה

הקריאה הראשונה עבדה (12 קבצים), והשנייה — לאותו `ref` בדיוק — החזירה
"Uploaded 20 file(s)" ו**לא העלתה כלום**. Plupload בונה מחדש את ה-`input`
אחרי כל אצווה, ולכן ה-`ref` הישן מצביע על אלמנט שנעקר מה-DOM. אין שגיאה.

⇒ **הזרימה היא: `navigate` ל-`media-new.php` ⇒ `find` ⇒ `file_upload`**, לכל
אצווה מחדש. עד 12 קבצים לאצווה עבד יציב.

### 💣💣 מונה הפריטים בדף ההעלאה הוא **DOM ישן ואינו הוכחה**

אחרי האצווה שנכשלה, `#media-items .media-item` החזיר "8 items · errors: 0"
בזמן שהסשן של wp-admin כבר פג ו**אף קובץ לא נכתב**. הדף הציג את שרידי
האצווה הקודמת.

⇒ **ההוכחה היחידה היא `wp_list_media`** — עם `search` על שם הקובץ, או
רשימה כללית שממנה רואים את המזהים החדשים. זהה לכלל "ok: true אינו הוכחה".

### ✅ ולריבועי הצבע עדיף בכלל לא דפדפן

קובץ swatch הוא 100–300 בתים, ולכן `wp_upload_media` עם `base64` עולה
כמה מאות טוקנים בלבד ועובד גם כשה-wp-admin מנותק. **תמונות מוצר** (20–40KB)
נשארות ב-`file_upload`, כי base64 שלהן היה עולה ~290 אלף טוקנים ל-32 תמונות.

### ⚠️ הסשן של wp-admin פג באמצע ריצה ארוכה

נמדד באותה ריצה: אחרי ~20 דקות של עבודה הדפדפן הופנה ל-`wp-login.php`.
אין להקליד סיסמה — עוצרים, מבקשים מדרור להתחבר, וממשיכים. ה-MCP של
וורדפרס **אינו מושפע** מזה ואפשר להמשיך איתו ליצירת מוצרים וואריאציות.

## 15. ריבוע הצבע — שתי מלכודות נוספות (13/09/2026, דגמי 1D)

1. 💣💣 **הדוגמן לובש בגד תחתון בצבע אחר.** ל-`1D352-080 ROYAL` יצא ריבוע
   `rgb(207,61,66)` — אדום, מהמכנסיים. הריבוע נראה תקין לחלוטין בפני עצמו.
2. 💣 **ואז הסריקה נחתה על הפנים** — `rgb(159,122,113)`, גוון עור.

⇒ `tools/site-swatch.js` סורק עכשיו **חלון גו** בלבד: `SCAN_BOTTOM=0.20` עד
`SCAN_TOP=0.60`, ו-`SCAN_SIDE=0.22` מכל צד. לאביזרים — `SCAN_TOP=1`.
⇒ צבע כהה אמיתי (BLACK, NAVY) דורש `MIN_LUM=0`, ובגד **לבן** דורש
`ANY_PIXEL=1` עם חלון צר, כי הוא אינו ניתן להבחנה מרקע הסטודיו.
⇒ **הבדיקה היחידה שתופסת את זה: להשוות את ה-RGB לשם הצבע.** לא להסתכל על
הריבוע.

⚠️ **ומונח `pa_color` קיים כבר נושא `ux_image` משלו.** `50-BLACK` (224)
משמש 6 מוצרים והחזיק 66712 — לא נגענו. רק מונחים חדשים קיבלו ריבוע.

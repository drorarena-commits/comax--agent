# הקמת פריטי ארנה — מצב העבודה

עדכון אחרון: **07/09/2026**. המסמך הזה הוא נקודת הכניסה לכל מי שממשיך את
העבודה על סוכן הקמת הפריטים — הוא מחזיק את מה שנמדד ואומת, את מה שדרור הכריע,
ואת מה שנשאר פתוח.

---

**הסוכן שמחזיק את כל זה: [`comax-items`](../.claude/agents/comax-items.md)** —
סוכן Claude ייעודי להקמת פריטים. המסמך הזה הוא הידע; הסוכן הוא מי שקורא אותו
לפני שהוא נוגע בקומקס.

## המטרה

סוכן ייעודי שמלווה פריט חדש מהרגע שנקנה ועד שהוא קיים בכל המערכות:

1. **הקמה בפריוריטי** של ספורט אנד מור — קובץ בפורמט שרויטל קולטת (מוצרי אב + בנים).
2. **הקמה בקומקס** — דרך **יבוא מאקסל שקיים במסך הפריטים**.
3. **הקמה באתר** (ווקומרס של ארנה ישראל) — עתידי, לא התחלנו.

**וסגירת מעגל** — הכשל שהניע את הכל: ארבע פעמים מאז מאי 2026 דרור ביקש מרויטל
להקים פריטים, היא הקימה בפריוריטי, ואצלו בקומקס הם לא הוקמו. אף אחד לא ידע.

---

## שלוש המערכות והיחס ביניהן

| מערכת | מה היא | המק"ט שלה |
|---|---|---|
| **ארנה איטליה** | המאסטר — קטלוג ופרייס-ליסט של היצרן | SKU + EAN |
| **פריוריטי** (ספורט אנד מור) | השותף העסקי | שרשור דגם+צבע+מידה, הצבע `00` |
| **קומקס** (של דרור) | | **מק"ט == ברקוד** |

**הכלל היחיד שחייב להתקיים:** `ספורט אנד מור (ארנה) ⊆ קומקס`. פריט שיש אצלם
ואין אצלו — ממצא. ההפך — תקין.

⚠️ **הצלבה תמיד לפי ברקוד.** לפי מק"ט נמצאה התאמה אחת מתוך 8,958.

---

## הנוסחאות — נמדדו, לא נוחשו

### עלות
```
FOB אחרי הנחות (מהאקסל של אליסה) × 1.45 × 3.8 = העלות בשקלים
```
נמדד על **772 שורות רכישה אמיתיות** (`דוח רכישות מספורט אנד מור`, 02–08/2026):
חציון **5.510**, רבעונים 5.51–5.51, **91% בטווח 2%**.

הפירוק: FOB ⇒ עלות שלהם = **×3.8 בלבד** (המרת מטבע, בלי מרווח). עלות שלהם ⇒
העלות של דרור = **×1.45** (המרווח שלהם עליו). והמחירון של ארנה ⇒ FOB בפועל =
**×0.468** (ההנחות שלהם).

⚠️ פריטים ישנים (2018–2024) לא יושבים על הנוסחה — **מורשת, לא כלל.** אין להסיק
מהם נוסחה חלופית.

### מחיר צרכן
**אין נוסחה קשיחה.** מה שנמדד:

- מחיר מחירון 1 של דרור ÷ מחיר הצרכן של ספורט אנד מור = **חציון 1.000** (n=1,756).
  **דרור מתמחר בדיוק כמוהם.**
- מחיר מחירון 1 ÷ העלות = חציון **3.327** (רבעונים 2.76–3.92).
- ספורט אנד מור בונים: `RSP € × 3.8` ⇒ עיגול ידני ⇒ `×1.18` מע"מ. שרשרת ה-1.18
  מדויקת ב-100%; **העיגול הוא החלטה אנושית**, ולכן רק 26% נופלים על הנוסחה.
- עיגול: **‎.90 או מספר שלם** — 98% מהמחירים בשתי המערכות.

**סדר העדיפויות שדרור אישר:** מחיר הצרכן של ספורט אנד מור ⇒ ואם אין, מחיר של
פריט דומה אצלו ⇒ ואם אין, `עלות × 3.327`. **מחיר סיטונאי = 50% מהצרכן.**

---

## מיפוי השדות לקומקס — אושר על ידי דרור

| שדה בקומקס | מקור | אימות |
|---|---|---|
| `פריט` = `ברקוד` | ה-EAN | 13,098/13,107 |
| `קוד חלופי` | **המק"ט של ספורט אנד מור כפי שהוא** | החלטת דרור |
| `דגם` | מספר הדגם, בלי צבע | == Style של ארנה ב-98% |
| `צבע` | **כפי שהוא מגיע מהמקור — בלי ריפוד** (הכרעת דרור 07/09) | == Colorway ב-100% |
| `שם פריט` | **נקי מקידומת המידה** | |
| `שם פריט באנגלית` | **תיאור הצבע, בלי קוד הצבע בראשו** | == Colorway Description ב-82% |
| `ספק` | **תמיד `121012` ספורט אנד מור** | פריטי 2026 |

⚠️ **מלכודות שעולות בדם:**

1. **בייצוא הפריטים מקומקס תוויות העמודות מוסטות.** מה שכתוב `מידה` מחזיק את
   **הצבע**, ומה שכתוב `מותג` מחזיק את **המידה**. לקרוא לפי מיקום (`KCOL`).
2. **ספק MGS (121001) הוא המפיץ הישן.** פריטי ארנה עד 2025 רשומים עליו. פריט
   חדש שיירש ספק מ"דגם זהה" ייכנס על המפיץ הלא נכון.
3. **`210 · ציוד` היא סכימת הסיווג הפעילה**; `14 · אביזרי שחיה` היא מורשת.
4. **הסיווג של ספורט אנד מור אינו מקור סמכות** — `ARENA HYDROSOFT PLUS` יושב
   אצלם תחת "כובעי שחייה" והוא **כפכף** (ארנה: `Equipment / Footwear`, סרגל
   מידות "יורו. 19<42").

---

## מה נבנה

| כלי | מה עושה |
|---|---|
| `src/tasks/items-export.js` | מייצא את כל הקטלוג מקומקס (a84 → נוספים → יצוא לאקסל). ברירת מחדל 37 עמודות |
| `tools/xlsx.js` | קורא xlsx ל-CSV בלי ספריות |
| `tools/xlsx-write.js` | כותב xlsx אמיתי, רב-גיליונות, עם צביעה וסינון |
| `tools/drive-get.js` | מוריד מגוגל דרייב דרך חלון הסוכן, בכל גודל |
| `src/items/build-import.js` | מרכיב את שורות ההקמה משלושת המקורות |
| `src/items/master-gate.js` | שער הדגמים והצבעים — מה חסר בקומקס לפני היבוא |
| `tools/items-import-file.js` | בונה את קובץ ההקמה + מריץ את כל שערי האימות |

**דורש התחברות חד-פעמית לגוגל** בחלון הסוכן כדי ש-`drive-get` יעבוד. נעשתה
במחשב הזה; במחשב השני תידרש שוב (הפרופיל אינו בגיט).

---

## התוצרים שעל הדיסק

| קובץ ב-`data/exports/` | תוכן |
|---|---|
| `הקמה-קומקס-175.xlsx` | **התוצר המרכזי** — 175 פריטים מוכנים, 22 עמודות, + גיליון `מאסטר חסר` |
| `175-פריטים-להקמה-מחירים-סופיים.xlsx` | מחירים מאושרים לכל 175 |
| `חסרים-אצלי-מספורט-אנד-מור.xlsx` | 809 החסרים, בשלוש רמות עדיפות |
| `56-פריטים-הצעת-מחיר.xlsx` | ההצעות לפריטים שלא היה להם מחיר בשום מקום |
| `SAM/` · `SAM-PUR/` | הגיליונות המומרים של מקורות ספורט אנד מור |

`content/פריטים-מלא-2026-09-07.csv` — הקטלוג של קומקס, 13,107 × 37.

⚠️ **קטלוגי ארנה עברו ל-`content/arena-catalogs/`** ולכן **נשמרים בגיט**.
`data/` אינו מגובה, וקובץ שיושב רק שם ייעלם במעבר בין מחשבים — וזה בדיוק
המקור היחיד לתיאורי הצבע. הסנכרון: `npm run catalogs-sync`.

---

## הממצא

**809 פריטי ארנה פעילים קיימים בספורט אנד מור וחסרים בקומקס.**

| עדיפות | פריטים |
|---|---|
| הוקם אצלם ב-2026 | **175** ⬅ העבודה |
| זז אצלם בשנה האחרונה | 105 |
| ישן/רדום | 529 — **דרור: "לא מעניין"** |

מתוך ה-175, **63 הם פריטים שדרור עצמו יזם** בארבעה מיילים לרויטל: 28/05 (21
פריטי POWERFIN PRO II FLEX), 06/08 (28 POWERSKIN AIR3 LE), 09/08 (5 HYDROSOFT
PLUS), 24/08 (19 מתוך 20 שביקש).

---

## שער המאסטר — נמדד 07/09/2026

בקומקס **`דגם` ו`צבע` הם ישויות מאסטר נפרדות מהפריט**. פריט שנקלט עם קוד שלא
הוקם — יידחה או ייצור ערך יתום. לכן קובץ ההקמה נבדק מול הקטלוג שייצאנו **לפני**
שנוגעים במסך היבוא. הפירוט המלא ב-[`MAP.md`](MAP.md), פרק "מסד הצבעים והדגמים".

| בדיקה מול 175 שורות ההקמה | נמצא חסר | מצב |
|---|---|---|
| **דגמים** | 14 — `006642 007964 010644 011643 011667 011674 011676 011806 011878 011879 011938 012062 012064 012937` | ✅ הוקמו |
| **צבעים** | 2 — `206` `220` (ועוד 2 שהתבררו כקיימים אחרי ביטול הריפוד) | ✅ הוקמו |
| שורות שנוגעות בערך חסר | היו 115 | **0/175** ✅ |
| מידות · מחלקות · קבוצות · קבוצות משנה · ספקים | 0 | ✅ |

השער: [`src/items/master-gate.js`](../src/items/master-gate.js), נקרא מ-
`tools/items-import-file.js` כשער **חוסם**. הגיליון `מאסטר חסר` נכתב לתוך
`הקמה-קומקס-175.xlsx`; שורה צהובה = יש בקומקס קוד **דומה ולא זהה**, אזהרה למי
שמקים ולא פטור מהקמה.

### הכרעת הריפוד — 07/09/2026

מסד הצבעים של קומקס מחזיק **196 קודים בני 3 ספרות ו-55 בני 1–2 במקביל**; `75`
יושב על 119 פריטים ו-`075` **אינו קיים כלל**. הקוד רופד כל צבע דו-ספרתי ל-3
ספרות, ולכן שבע שורות נשלחו לצבע שאינו קיים במקום לזה שכן.

**דרור: הצבע נכתב כפי שהוא מגיע מהמקור.** `padColor` הוסרה מבניית השורה ונשארה
רק כהתאמה פנימית מול קטלוג ארנה, עם איסור מפורש לגעת בערך שנכתב. התוצאה: 7
שורות (`075`⇒`75` ×6, `050`⇒`50` ×1) התחברו לצבעים הקיימים, והצבעים להקמה ירדו
מ-4 ל-2.

⚠️ **והמקור הוא `פריט מרכז/דגם`, לא הקוד החלופי.** הקוד החלופי הוא המק"ט של
ספורט אנד מור (דגם+צבע+מידה) והחיתוך בו נופל על ספרות אחרות לגמרי — טעות שעלתה
לי בדיקת שקילות שגויה שדיווחה "הריפוד לא נגע באף שורה" בזמן שנגע בשבע.

### 💣 חוב ידוע: חיתוך הדגם והצבע

`פריט מרכז/דגם` הוא שרשור **בלי מפריד** של דגם וצבע, והחיתוך
`slice(0,6)/slice(6,9)` מניח דגם בן 6. **ההנחה נכונה רק כשהקוד באורך 9.**

| מה קורה | דוגמה |
|---|---|
| אורך 7–8 — הדגם גונב מהצבע | `1E048503` ⇒ דגם `1E0485` צבע `03`, במקום `1E048`/`503` |
| אורך 10+ — **תווים נזרקים** | `2026550012` ⇒ דגם `202655` צבע `001`, והתו האחרון נעלם |

**מספרים שנמדדו 07/09/2026 — אחרי היבוא של 175:**

| | |
|---|---|
| 175 שהוקמו | ✅ **נקיים.** 168 באורך 9, ו-7 באורך 8 שנבדקו ידנית ויצאו נכון |
| פריטים פעילים שעדיין חסרים | 634 |
| מהם עם קוד באורך חריג | **468** (7 תווים ×13 · 8 ×189 · 10 ×228 · 11 ×38) |
| **מהם שיעברו את שער המאסטר בשקט** | **0** — בכל 468 הדגם השגוי אינו קיים בקומקס |

⚠️ **המספר "471 מתוך 2,633" שהופיע כאן קודם תיאר שורות בקובץ המקור, לא פריטים
פגומים.** אין פריטים פגומים בקומקס.

**דרור הכריע: הפיצול המלא נדחה למנה הבאה** — אין נזק קיים והשער חוסם 468/468.
מה שכן נסגר: **השער מסמן `suspectSplit`** ואומר מפורשות *אל תקים את הדגם הזה*,
כי הסיכון האמיתי הוא שמישהו יקים דגם מומצא רק כדי שהשער יעבור.

**וכשסוגרים את החוב** — הסמכות היא `Style`/`Colorway` של ארנה לפי EAN, ומתחתיה
מסד הדגמים של קומקס (לנסות חיתוך ב-6 ואז ב-5, ולבחור את זה שקיים). ⚠️ שני
חיתוכים שנותנים דגם קיים אינם הכרעה — זו שורה שהולכת ל"דורש החלטה".

---

## מצב הסנכרון מול האתר אחרי הקמת 10 הכובעים — נמדד 08/09/2026

השאלה של דרור: האם 10 הפריטים שהוקמו מופיעים באתר **עם אותם מק"טים**.
נמדד מול `arenaisrael.co.il` דרך ה-WordPress MCP — `product_variation`, השוואת
**מחרוזת מדויקת** מול `פריט`/`ברקוד`/`קוד חלופי` בייצוא הקומקס. **4 מתוך 10.**

### `CLASSIC SILICONE` (דגם 91662, מוצר 69228 באתר) — 8 וריאציות, 4 עודכנו

| צבע באתר | מק"ט באתר | בקומקס | מצב |
|---|---|---|---|
| BLACK-055 · WHITE-015 · SILVER-051 · NAVY-071 | `9166205 5/015/051/071` | קיים | ✅ תואם |
| 103-pink | `3468333912112-69230` | **לא קיים** | ❌ הזמנה תיפול בשקט |
| 35-yellow | `3468333912112-69231` | **לא קיים** | ❌ הזמנה תיפול בשקט |
| 75-Navy-gray | `3468333912112-69232` | **לא קיים** | ❌ הזמנה תיפול בשקט |
| 65-LIME-SMOKE | `3468333912112` | קיים — אבל זה **"כובע סיליקון מיקס2"** (צבע 90) | ⛔ גרוע יותר: ההזמנה **כן** תעבור ותוריד מלאי מפריט המיקס |

השורה האחרונה היא הכשל הגרוע מכולם, כי הוא **לא נראה ככשל**: אין הזמנה שנופלת,
יש רק מלאי שיורד מהפריט הלא נכון. זו בדיוק תבנית "ברקוד פיזי אחד לקופסת מיקס"
המתועדת ב-`mix-box-shared-barcode-pattern`.

### `כובע דגל ישראל` (דגם 123456, מוצר 75658 באתר) — 2 וריאציות, **0 תואמות**

| צבע | מק"ט באתר | מה הוקם בקומקס | מצב |
|---|---|---|---|
| 100-לבן | `123456-100` | `123456100` | ❌ **מקף** |
| 800-כחול | `123456-800` | `123456800` | ❌ **מקף** |

⛔ **ההפרש הוא תו אחד, והוא מספיק.** תוסף הסנכרון משווה מחרוזות, ולכן
`123456-100` אינו `123456100`. זו בדיוק המלכודת ש-`never-normalize-source-values`
מזהיר מפניה, רק בכיוון ההפוך: **קובץ ההקמה הוא שהוריד את המקף.** גם ל**מוצר
האב** עצמו יש מק"ט `123456`, שאינו קיים בקומקס.

**הלקח:** להקים פריט לפריט־אתר קיים **בלי לקרוא קודם את המק"ט שבאתר** הוא ניחוש.
השוואת המחרוזת חייבת לקרות **לפני** בניית קובץ ההקמה, לא אחריה.

### ✅ 6 המק"טים עודכנו באתר — ו-`comax_id` נחשף כשדה שני שלא ידענו עליו (08/09/2026)

באישור מפורש של דרור עודכן `_sku` בשש וריאציות דרך ה-WordPress MCP:

| וריאציה | היה | הפך ל- |
|---|---|---|
| 69230 · 103-pink | `3468333912112-69230` | `91662103` |
| 69231 · 35-yellow | `3468333912112-69231` | `91662035` |
| 69232 · 75-Navy-gray | `3468333912112-69232` | `91662075` |
| 69229 · 65-LIME-SMOKE | `3468333912112` | `91662065` |
| 75664 · 100-לבן | `123456-100` | `123456100` |
| 75659 · 800-כחול | `123456-800` | `123456800` |

**אומת: 10 מתוך 10** — שאילתה חוזרת על כל עשרת המק"טים החזירה את כל
הווריאציות.

### ⚠️ ⚠️ אבל יש שדה מטא שני, `comax_id`, והוא **לא** התעדכן

בכל וריאציה יושב `comax_id` לצד `_sku`. מדגם של 40 הווריאציות שעודכנו
אחרונות:

| ממצא | כמות |
|---|---|
| יש `comax_id` | 30 מתוך 40 |
| מהן `comax_id` **זהה** ל-SKU | 23 |
| `comax_id` **שונה** מה-SKU | 7 — **וחמש מהן נוצרו היום** |

כלומר **המצב התקין הוא `comax_id == _sku`**, וזה לא שדה מורשת. עדכון `_sku`
לבדו יצר ארבע שורות שבהן השניים חלוקים:

| וריאציה | `_sku` (חדש) | `comax_id` (נשאר) | מה זה בקומקס |
|---|---|---|---|
| 69229 · 69230 · 69231 · 69232 | `9166206 5/103/035/075` | `3468333912112` | **"כובע סיליקון מיקס2"** — פריט המיקס |
| 75659 · 800-כחול | `123456800` | `3468336990391` | שריד משכפול של כובע Bruno Fratus (גם `_parent_sku=006806`) |
| 75664 · 100-לבן | `123456100` | **אין** | — |

⛔ **לא נגעתי ב-`comax_id`** — לא ידוע אם התוסף קורא אותו, כותב אותו, או
שניהם, וניחוש כאן הוא בדיוק סוג הכתיבה שכלל 9 אוסר.

### ⚠️ ונלווה: ארבע הווריאציות ש"סוכן האתר" תיקן בפברואר אינן מנוהלות מלאי

`74463`–`74466` (BLACK-055 · WHITE-015 · SILVER-051 · NAVY-071) נושאות
`_sku` תקין, אבל **`_manage_stock: "no"`, `_stock: null`, ובלי `comax_id`** —
בעוד **40 מתוך 40** הווריאציות במדגם הן `_manage_stock: "yes"`. הן יוצגו
"במלאי" תמיד, ללא תלות בקומקס. ייתכן שזו הכוונה וייתכן שזה חור — **צריך
הכרעה של דרור, לא ניחוש.**

## מה נשאר פתוח

0. ✅ **המאסטר הוקם** (07/09/2026) — 2 צבעים ו-14 דגמים, ושער המאסטר עובר
   לראשונה: `0/175` שורות נוגעות בערך חסר. המשימות: `color-create` (a125)
   ו-`model-create` (a77).
1. ✅ **מסך היבוא מאקסל מופה** (07/09/2026) — `a84` → לשונית "נוספים" →
   `#ImpExl`, סוג יבוא `0 · פריטים`, 13 עמודות. הפירוט המלא ב-
   [`MAP.md`](MAP.md), פרק "יבוא פריטים מאקסל". המשימה:
   [`src/tasks/items-import.js`](../src/tasks/items-import.js) —
   `npm run items-import`, עוצרת לפני הקליטה כברירת מחדל.

   ✅ **וההרצה בוצעה — 175 פריטים נקלטו** (07/09/2026, 18:33). דרור בדק את
   הקובץ מול המסך ואישר יבוא מלא. האימות: `items-export` החזיר **13,282
   פריטים מול 13,107 קודם — הפרש 175 בדיוק**, וכל 13 השדות הממופים תאמו
   ב-175/175 (ברקוד · מק"ט · קוד חלופי · שם · שם באנגלית · דגם · מחלקה ·
   קבוצה · קבוצת משנה · ספק · צבע · מידה · מחיר).

   💣 **ובמעבר המשותף על המסך נתפס כשל שהיה הורס את ההרצה:** כל 175
   השורות נכתבו עם הדגם בתא **מספרי** (`002507` ⇒ אקסל מציג `2507`), ושער
   המאסטר עבר `0/175` בשמחה כי הוא בודק ערכים ולא סוגי תא. תוקן ב-
   `xlsx-write.js`, ונוסף שער `numericCells()`. ראו `MAP.md`, "סוג התא באקסל".

   💣 **ובאצ' של 10 (08/09/2026) חשף שהיבוא היה שבור לגמרי מאז — חמש הרצות
   רצופות נתקעו על ה-V הירוק השני.** הקוד קרא את `page.frames()` **פעם
   אחת**, לפני ההמתנה, ופריימי שתי הרשתות נולדים רק אחרי הקליק הראשון —
   ולכן הספירה החזירה `0 · 0` לנצח, בין אם המתינו 6 שניות ובין אם 60.
   בנוסף, המונה במסך הזה כתוב `סה"כ פריטים: 10` **בלי "מתוך"**, ולכן
   הביטוי הרגולרי שהועתק מ-`cost-import` לא התאים לעולם. שניהם תוקנו.

   ⚠️ **וה"logoff" שנראה במקום הקליק הוא ההתאוששות:** `tools/run.js` עושה
   לוגין מחדש על כל כישלון במשימת כתיבה, ולכן הדפדפן ניווט למסך ההתחברות
   בדיוק ברגע שהיה אמור ללחוץ. **לוגין פתאומי באמצע זרימה = לחפש את
   השגיאה שקדמה לו, לא לחשוד במושב.** הפירוט המלא ב-[`MAP.md`](MAP.md).

   ✅ **ונולד דגל `previewOnly: true`** — מריץ את הזרימה עד מסך התוצאה,
   מדווח כמה תקין וכמה לא תקין, ו**לא נוגע בכפתור הקליטה**. אי אפשר ליצור
   בו פריט בטעות, ולכן זו הדרך לבדוק שינוי בקוד היבוא מול המסך האמיתי.

   ✅ **ההרצה החיה, 08/09/2026: 10 כובעים נקלטו** — 10 תקין · 0 לא תקין,
   ו-`items-export` טרי החזיר **13,292 מול 13,282** (הפרש 10 בדיוק), עם `grep`
   שהחזיר 1 בכל אחד מ-10 הברקודים. ⚠️ ו-`stillOpen` (מסך התוצאה נשאר פתוח)
   התברר כ**התראת שווא** — הוא נשאר פתוח גם בהצלחה, והמבדיל היחיד מול כישלון
   הוא רצועת "כפילות בנתונים !".

   ⚠️ **ומסך הקמת/עדכון פריט (`PrtU`) עדיין לא ממופה** — פריט בודד עדיין
   אי אפשר להקים או לתקן ידנית דרך הסוכן.
2. **תיאור הצבע מולא ב-171 מתוך 175** (07/09/2026) — ראו "מאיפה מגיע תיאור
   הצבע". 20 השורות שנותרו הן Racing & Team Line, והמקור להן הוא קובץ שטרם
   הובא.
3. **דוח מחירונים מפריוריטי** — כרטיס הפריט שלהם לא מכיל מחירון סיטונאי
   (1 מתוך 2,634), כלומר המחירונים הם ישות נפרדת שעוד לא ראינו. ייתכן ששם
   מתומחרים 56 הפריטים שנראים חסרי מחיר.
4. **חוב ידוע:** `POWERSKIN CARBON AIR2 LE JAMMER` קיבל `141264 · תחרותי` בעוד
   ששאר ה-JAMMER קיבלו `20015 · טיץ שחייה`. דרור: לאחד בהמשך.
5. **שלב האתר — התחיל, ונתקע פעם אחת.** דרור אישר יבוא של `הקמה-10-פריטי-אתר.xlsx`
   (דגם `91662` בשבעה צבעים חדשים + דגם `123456` בשני צבעים קיימים).
   ⛔ **08/09/2026: ה-`--confirm` הראשון נכשל בשקט** — מסך התוצאה נבנה
   אסינכרונית (בדיוק כמו מלכודת 3 בפרק מחירי העלות), הקוד לחץ על כפתור
   הקליטה לפני שהתייצב, וקומקס החזיר "כפילות בנתונים !". הכלי דיווח
   `ok: true` בכל זאת; `items-export` טרי + `grep` על 10 הברקודים אישרו
   **0 מתוך 10 נוצרו**. שבעת הצבעים החדשים (`055/071/015/051/035/075/065`)
   כן נוצרו כבר ב-`color-create` ונשארים תקינים — הכשל הוא רק בשלב הקליטה
   של הפריטים. **התיקון בקוד** (`src/tasks/items-import.js`): ממתינים
   שהמונים יתייצבו, וזורקים שגיאה על `stillOpen` ועל "כפילות בנתונים" במקום
   להזהיר. **הצעד הבא: להריץ שוב `items-import` על אותו קובץ** — אין צורך
   בהקמת צבעים חוזרת. פירוט מלא ב-`MAP.md`, "יבוא פריטים מאקסל".

---

## איך דרור רוצה לעבוד

לפרק שלב-שלב יחד. **לעצור על ממצא ולא להציע את הצעד הבא מיוזמה** — אבל **בתוך
מה שכבר נתבקש, לא לעצור ולשאול רשות.** היעד הוא רוטינה חוזרת, לא תוצר חד-פעמי.

---

## מאיפה מגיע תיאור הצבע — נמדד 07/09/2026

⚠️ **הקוד יורד מראש התיאור.** ארנה ואליסה כותבות `222-NEON BLAZE`, ובקומקס
הקוד כבר יושב בשדה `צבע` נפרד — הכפילות מיותרת (הכרעת דרור). `stripColorCode()`
מסיר את הקידומת **רק כשהמספר הוא באמת קוד הצבע של אותה שורה**, כי תיאור
שמתחיל במספר אחר אינו קידומת אלא חלק מהשם. נמדד: 126 מתוך 171 נשאו קידומת,
וכולן תאמו את הקוד. שער ב-`items-import-file.js` מוודא שלא נשאר אף אחד.

`שם פריט באנגלית` בקובץ ההקמה הוא **תיאור הצבע**, והוא נשלף מקבצים שארנה
ואליסה שולחות במייל. המקור השלישי, האחרון בסדר האמינות, הוא **הקטלוג של קומקס עצמו**: פריט קיים
באותו דגם+צבע כבר נושא את התיאור. נמדד — `011584|222` לא היה באף קטלוג של
ארנה, ובקומקס ישבו 9 פריטים עם `222-NEON BLAZE`. זו קריאה ממקור קיים ולא
ניחוש, ולכן היא לגיטימית — אבל אחרונה, כי קומקס הוא ההעתק ולא המקור.

`arenaCatalogs()` מזהה כל CSV ב-`data/exports` (עומק 2)
שיש בכותרתו עמודת ברקוד ועמודת תיאור צבע — אין צורך לגעת בקוד כשמוסיפים קובץ,
רק להמיר אותו ב-`tools/xlsx.js`.

| הקובץ | מה הוא מכסה | שמות העמודות |
|---|---|---|
| `AINT_<עונה>_Master Data` | **הקטלוג העונתי** — ביגוד · בגדי ים · תיקים | `EAN` · `Style` · `Colorway` · `Colorway Description` |
| `2026 Racing and Team Line \| Master Data` | **פריטי תחרות** — POWERSKIN · POWERFIN · כובעים | כנ"ל |
| דוח הזמנות מאליסה | **מה שנקנה בפועל**, חוצה עונות ומשפחות | `EAN/UPC` · `Style code` · `Color Code` · `Color Description` |
| `<שם> Limited Edition_Master Data` | **מהדורה מוגבלת** — קובץ נפרד לכל LE | `EAN` · `Style` · `Colorway` · `Colorway Description`, **כותרת בשורה 3** |

⛔ **הקטלוג העונתי אינו מכסה פריטי תחרות.** נמדד: מתוך 21 הדגמים שחסר להם
תיאור, `AINT_FW26_Master Data` (7,477 שורות) הכיל **שניים**, ומילא **אפס**
שורות. 19 הנותרים — `POWERSKIN ST NEXT`, `AIR3`, `POWERFIN PRO II FLEX`,
כובעי סיליקון — יושבים בקובץ ה-Racing השנתי. **לפני שמחפשים קובץ, לזהות לאיזו
משפחה הפריט שייך.**

✅ **דוח ההזמנות של אליסה הוא המקור החזק ביותר** לפריטים שנקנו: 144 שורות
מילאו **104** מתוך 145 החסרים, כי הוא נגזר מהזמנות אמיתיות ולא מקטלוג.

⚠️ **שתי מלכודות בזיהוי הקובץ**, שתיהן שקטות ושתיהן מטופלות ב-`CATALOG_COLS`
ו-`headerRow()`:
1. **שמות עמודות שונים בין המקורות** — `EAN` מול `EAN/UPC`,
   `Colorway Description` מול `Color Description`.
2. **שורת הכותרת אינה תמיד הראשונה** — ב-`AINT_FW26_Master Data` שורה 1 היא
   שם הקובץ ושורת העמודות היא השנייה.

### מה שנשאר חסר — 4 שורות, וכולן ייצור מותאם

`006642|400` MOULDED EMEK ISR · `007964|100` FLAT SILICONE HERZLIYA ISR ·
`010644|700` MOULDED IRONI MODIIN · `012937|501` 3D SOFT LION

⛔ **אין טעם לחפש להן קובץ.** אלה כובעים בייצור מותאם למועדונים בישראל —
עמק, הרצליה, עירוני מודיעין — ולכן אין להם שורה בקטלוג היצרן. סריקה של כל
46 הקבצים שעל הדיסק מצאה את הברקודים שלהם **רק** בקבצים של ספורט אנד מור,
ששם שדה הצבע הוא `00`. תיאור הצבע שלהם יכול להגיע מדרור בלבד.

**גרסאות של אותו קובץ אינן זהות.** `AQUA GLEAM..._Distributors2` הכיל את
`006344|508` שחסר בגרסה הראשונה — 7 שורות. כשמגיע קובץ בשם דומה, להמיר
אותו לאותה תיקייה ולהריץ שוב; הוא דורס ומרחיב.

# סוכן הצעת מחיר — `Doc612` / `a164`

**המסמך היחיד שמופה במלואו ומאומת בהרצה חיה** (01/09/2026). המנוע המשותף חולץ
ממנו, ולכן הוא גם אמת המידה: מסמך אחר שמתנהג אחרת — זה ההבדל שצריך לתעד.

## מצב המיפוי

| מסך | URL | מצב |
|---|---|---|
| רשימה | `Erp/Mehirot/Doc612/AzaaMhr/Doc612V.asp` | ✅ |
| כותרת | `Doc612U.asp` | ✅ |
| רשת שורות | `Doc612LinesV.asp` | ✅ |
| דיאלוג שורה | `Doc612LinesU.asp` | ✅ |

`knowledge/screens/`: `quote-new`, `quote-form`, `quote-lines`.

## המיוחד בו

- **כלום לא זז.** הצעה לא נוגעת במלאי ולא בהנהלת חשבונות. לכן זה המסמך הבטוח
  לחזור עליו זרימה לפני שמריצים אותה כחשבונית.
- **טיוטה שלא הופקה נתפסת מחדש.** `create` נוסף על אותו לקוח **יחזור להצעה
  הפתוחה** ויחליף לה את הכותרת במקום לפתוח חדשה. כך `6120045` עברה ממחירון
  קבוצות למכירה ראשי באמצע בדיקה. מפיקים לפני שמתחילים מסמך נוסף.
- **הדפסה חייבת להיות מושתקת.** כל ערך עותקים שאינו 0 גורם לקומקס לקרוא
  ל-`window.print()`, והדיאלוג של Chrome חוסם את CDP לחלוטין — הסוכן לא יכול
  אפילו להתחבר מחדש, רק אדם יכול לסגור. `quote-finalize` מגדיר 0 ומייצר את
  ה-PDF בעצמו מ-`Doc612_HtmlP_T13.asp`.
- **`#wPrt` קיים ברשימה** — סינון לפי פריט, ש-`Doc650V` לא מציע.

## תמחור סיטונאי — שתי דרכי הזנה, אותו כסף

| | מחירון לפני מע"מ | מחירון כולל מע"מ |
|---|---|---|
| מוזן | מחיר `round(ברוטו/2)`, הנחה 0 | המחיר נשאר, הנחה `(1 − יעד×(1+מע"מ)/ברוטו)×100` |
| שורה | 145.00 | 171.10 |
| **סה"כ** | **171.00** | **171.00** |

האחוז נגזר מהיעד המעוגל — 40.98% ל-289.90 ב-18%, לא 41% עגול. **המשימה עוצרת**
כשהיא לא יודעת אם המחירון כולל מע"מ, וקוראת את הנטו בחזרה מהשדות לפני שמירה.
המסמך מצהיר על המחירון בפוטר: `לפי מחירון: מכירה ראשי (כולל מע''מ)`.

## מלכודות שכבר עלו בדם

- **`#chg` הוא לא בורר** — הוא מחליף *סוג לקוח* ומחליף את השדה מתחת לידיים.
  הבורר האמיתי הוא `#CcomboBut<שם השדה>`.
- **מספר המסמך הוא `<span id="DocId">`**, לא input — ובטופס ההוספה הוא רק
  תצוגה מקדימה. פעם אחת הוא הראה `6120041`, שהתברר כהצעה קיימת מ-2025 של לקוח
  אחר. המספר האמיתי נקרא ממסך השורות.
- **הבורר מסנן לפי מה שכבר בשדה** — לנקות לפני שפותחים אותו, אחרת מקבלים
  תוצאה אחת ומסיקים ממנה מסקנה שגויה.
- **לחיצת אישור הכותרת נבלעת לפעמים** — ראה `MAP.md`.

## המשימות הקיימות

`quote-new` · `quote-add-line` · `quote-finalize` · `quote-email`.
הסוכן הזה עוטף את אותה זרימה דרך המנוע המשותף; ה-PDF וההדפסה נשארו
ב-`quote-finalize`, שמחזיק את הידע על השתקת ההדפסה.

## ⚡ a164 נפתח לפי נתיב — בשני מקומות, לא באחד (06/09/2026)

הפרופיל מגדיר `program: 'Erp/Mehirot/Doc612/AzaaMhr/Doc612V.asp'`, וזה מה
שמעביר את `openProgram` למסלול `top.S.runProgram` במקום להרים את שולחן העבודה
ולחפש אייקון. **אבל הפרופיל לבדו לא מספיק:** `quote-new` קורא ל-`openProgram`
ישירות ולא נוגע בפרופיל, ולכן הוא מייבא את `profile.program` במפורש. שתי נקודות
הפתיחה — `quote-new` ו-`document-email` — צריכות אותו.

| נקודת פתיחה | לפני | אחרי |
|---|---|---|
| `quote-new` | 40.8s | **2.5s** |
| `quote-email` | 22.3s | **2.9s** |

## 💣 `?SwVO=0&SwLk=1` בנתיב הוא קריטי — אל תנקה אותו

הנתיב בפרופיל **חייב** לשאת את הפרמטרים האלה. `runProgram` לא משחזר את מה
שהאייקון בשולחן מעביר, והחוסר **לא מתגלה בפתיחה**: המסך נפתח ב-2.5 שניות ונראה
תקין לגמרי, טופס הכותרת נפתח, מספר ההצעה מוצג — ורק בשלב מילוי הלקוח `#IdxLk`
נשאר **מוסתר** עד timeout של 30 שניות. שוחזר פעמיים על שולחן נקי, נפתר בהרצה
הראשונה אחרי הוספת `SwLk=1`. `Lk` = לקוח.

⚠️ **לכן אימות של נתיב חדש הוא זרימה מלאה, לא "המסך נפתח".** ראה `MAP.md`.

⚠️ נתיב שגוי גם לא זורק שגיאה: הוא ממצה `settle` + 4 `think` (~10–17.5s), נופל
למסלול השולחן, ומסיים **איטי יותר** מאשר בלי `program` בכלל.

## ⚠️ `quote-finalize` לא מזהה מספר מסמך לבד — להעביר `docNo`

נמדד 06/09/2026: בלי `docNo` מפורש הוא מדווח `לא זיהיתי מספר מסמך` ו**לא מפיק
PDF** — הקליטה עצמה כן עוברת. זה **לא** קשור לפתיחה לפי נתיב: בדיקת הלוגים מראה
שבכל ההרצות המוצלחות (02/09 ו-06/09) `docNo` הועבר במפורש, כלומר הזיהוי האוטומטי
מעולם לא הופעל. עד שימופה — להעביר `docNo` תמיד.

## ⚠️ סכום השורות לא שווה לסה"כ לפני מע"מ

קומקס מעגל את סה"כ המסמך לשקל שלם ורושם את ההפרש כהנחת מסמך זעירה — **גם
שלילית**. ב-6120050: שורה 2,398.85, הנחה −0.31, לפני מע"מ 2,399.16, סה"כ 2,831.00.
אימות שמשווה שורות מול סיכום ייכשל בשקל אם לא מחברים את הנחת העיגול.

## ⚠️ `#Pratim` מוגבל ל-40 תווים וקוצץ בשקט

נמדד 05/09/2026: מחרוזת פרטים בת 86 תווים נשמרה כ-40, בלי שגיאה ובלי סימן
במסך. שער האימות ב-`engine.js` (`assertFields`) תופס את זה ועוצר לפני שהכותרת
מאושרת — אבל עדיף לא להגיע לשם. **להשאיר `details` מתחת ל-40 תווים.**

לא נבדק אם אותה מגבלה חלה על מסמכים אחרים.

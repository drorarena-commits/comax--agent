# סוכן חשבונית מס/קבלה — `Doc652` / `a132`

חשבונית וקבלה במסמך אחד. **מזיז מלאי וגם גובה תשלום.**

## מצב המיפוי

| מסך | URL | מצב |
|---|---|---|
| רשימה | `Erp/Mehirot/Doc650/InvKab_Mlay/Doc652V.asp` | ✅ נקרא ע"י `customer-history` |
| כותרת | `Doc652U` | ❌ |
| שורות | `Doc652LinesV` / `Doc652LinesU` | ❌ |
| אמצעי תשלום | ? | ❌ |

## שלוש מלכודות ייחודיות

1. **הנתיב משקר.** המסמך יושב תחת התיקייה של **Doc650**:
   `Erp/Mehirot/Doc650/InvKab_Mlay/Doc652V.asp`. התאמת frame על "Doc650" תופסת
   גם אותו. לכן גוזרים את הקידומת מה-URL עם `/(Doc\d+)V\.asp/` ולא מניחים.
2. **`הנחה%` בלי רווח.** ב-`Doc650` וב-`Doc612` זה `הנחה %`. קריאת רשת לפי
   תווית עמודה נשברת בדיוק על התו הזה. תועד ב-`MAP.md`.
3. **הקליטה גובה תשלום.** המסמכים המכירתיים האחרים לא עושים את זה. השלב הזה
   לא מופה, ולכן `finalize` **מסרב** — קבלה חצי-קלוטה גרועה מכלום.

## מה כן יש

**`#wPrt` ברשימה** — סינון לפי פריט, ש-`Doc650V` לא מציע. שווה שימוש: זה עונה
על "באיזו חשבונית היה הפריט הזה" בלי לפתוח מסמכים.

## איך למפות

```bash
npm run open-program -- a132
npm run snapshot -- invoice-receipt-header    # אחרי #newRec
npm run snapshot -- invoice-receipt-payment   # אחרי לחיצה על קליטה
```

מה שחשוב לתעד במסך התשלום: אילו אמצעי תשלום קיימים, מה חובה, ומה קורה כשסכום
התשלום לא תואם את סכום המסמך.

⚠️ `#newRec` יוצר מסמך אמיתי. לצאת ב-`backOut` (`#DoExit` → `#Cancel`).

## מתי משתמשים בו בפועל

בהיסטוריית הרכישות של לקוח 429028 הופיעה `1012804` מ-01/05/2026 כ"חשבונית
מס/קבלה" — כלומר דרור כן משתמש בזה. בדוח תנועות ללקוח סדרת המסמכים היא `10xxxxx`.

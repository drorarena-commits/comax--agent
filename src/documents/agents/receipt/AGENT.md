# סוכן קבלה — `a103`

הצד השני של הכסף. **לא מזיז מלאי — רושם תשלום שהתקבל ומשייך אותו לחשבוניות.**

**שתי קבלות אמיתיות נקלטו ב-03/09/2026: 6800005 (דרור הקליק) ו-6800007 (הקוד
הריץ, `receipt-drive.mjs`).** שתיהן על לקוח 112001, מזומן, ₪1, שויכו ואומתו.

המקורי: **הזרימה אומתה מקצה לקצה על 6800005** — לקוח 112001 (דרור כהן
מנהל), מזומן, ₪1, שויכה לחשבונית פתוחה ואומתה ברשימה. **דרור הקליק כל שלב;
הסוכן צילם כל מסך.**

`mapped` הוא `{ list: true, header: true, lines: false }` — ו-`lines` יישאר `false`
לנצח, כי למסמך הזה אין מסך שורות (`hasLines: false`).

⚠️ **אבל `driven: false`, ו-`create` עדיין מסרב.** הקוד כאן נכתב מתוך התצפית
ומעולם לא הריץ קבלה בעצמו. "ממופה" ו-"עובד" הן שתי טענות שונות, והשנייה עוד לא
נכונה.

---

## ⚠️ הכלל הראשון: הסוכן לא נוגע בפרטי כרטיס אשראי

מסך הכותרת מכיל **מספר כרטיס אשראי, תוקף, CVV ומספר אישור**. `#CvvKab` הוא
`input[type=password]`.

```js
export const NEVER_FILL = ['#AshraiNum', '#AshraiToDate', '#CvvKab', '#IshurAshrai'];
```

**ארבעת השדות האלה נשארים לבני אדם — תמיד.** לא עם `--confirm`, לא בהוראה
מפורשת, ולא "רק לבדיקה". קומקס יקבל אותם משמחה מסקריפט, וזו בדיוק הסיבה לכתוב
את הכלל ולא להסתמך על זה שהוא לא יעלה.

מה שכן מותר לקרוא ולכתוב: הסכום, מספר התשלומים, חברת האשראי, סוג העסקה.

---

## המפה — נקרא חי 03/09/2026

| מסך | URL | מצב |
|---|---|---|
| רשימה | `Max2000/Kupa/Kab/Kab/KabalaV.asp` | ✅ נקרא |
| כותרת UPDATE | `Max2000/Kupa/Kab/KabAsh/KabalaNU.asp` | ✅ נקרא (7010013) |
| כותרת ADD | אותו URL, `DocMode=ADD` | ✅ נקרא (כותרת ירוקה = עדכון · כתומה = הוספה) |
| פרוט שיקים | `#goLines` פותח | ❌ לא נפתח |
| פרוט אשראי | `#goAshrai` פותח | ❌ לא נפתח |
| שיוך לחשבוניות | `#ShiuhHesh` ברשימה | ❌ לא נפתח |
| חשבון קופה | `#getKupa` בכותרת | ❌ לא נפתח |

סלקטורים: `receipt-list.*` · `receipt-header.*` (UPDATE) · `receipt-header-add.*` (ADD),
כולם תחת `knowledge/screens/`.

## ארבעה דברים שאי אפשר היה לגזור משכן

### 1. אין מספר Doc, ושני המסכים בתיקיות שונות

```
רשימה:  Max2000/Kupa/Kab/Kab/KabalaV.asp
כותרת:  Max2000/Kupa/Kab/KabAsh/KabalaNU.asp
```

לא `Doc470`, לא `Doc612`, לא `Doc652` — **בלי ספרות בכלל**, תחת `Kupa/` (קופה)
ולא תחת `Erp/Mehirot/`. וגם השם לא עקבי: `KabalaV` ברשימה, **`KabalaNU`**
(ולא `KabalaU`) בכותרת, בתיקייה אחרת.

התבנית `/Doc\d+V\.aspx?/i` שכל ארבעת הסוכנים האחרים משתמשים בה **לא הייתה
מתאימה לכלום**, והסוכן היה מדווח "מסך הרשימה לא נפתח" על מסך שנפתח מצוין.

```js
frames: { list: /KabalaV\.aspx?/i, header: /KabalaNU\.aspx?/i }
```

⚠️ **כפילות שם:** מסכי התשלום של `Doc652` נקראים `Kabala652_LinesNV.asp`.
התאמה רופפת על `/Kabala/` תתפוס אותם. הספרות מיד אחרי `Kabala` הן ההבדל.

### 2. אין מסך שורות — התשלום יושב על הכותרת

זה שובר את המבנה של כל ארבעת המסמכים האחרים. אין `<Doc>LinesV`, אין
`<Doc>LinesU`. הכותרת עצמה מחזיקה את סוג התשלום ואת בלוק האשראי, ושני כפתורים
פותחים תת-מסכים: `#goLines` (פרוט שיקים) ו-`#goAshrai` (פרוט אשראי).

### 3. ⚠️ ולכן `#OK` בכותרת כנראה **קולט**, ולא מתקדם

בארבעת המסמכים האחרים `#OK` בכותרת רק מתקדם לשורות, ו-`#OK` בשורות קולט. **כאן
אין שורות להתקדם אליהן.** מכאן שה-`#OK` של `KabalaNU` הוא ככל הנראה כפתור
הקליטה — **היפוך מדויק של כלל 4 ב-`CLAUDE.md`**.

**לא הוכח, ומטופל כאילו הוא קולט.** היציאה שנבדקה בפועל היא **`#Cancel`** —
היא סגרה את 7010013 בלי לכתוב כלום. `#DoExit` קיים גם הוא ולא נבדק.

### 4. אין פריטים — ועכשיו זה על המסך, לא בהיסק

שלושה סימנים יחד:

1. **`#ShiuhHesh`** ("שיוך לחשבוניות") ברשימה.
2. **אין `#wPrt`.** ל-`Doc652V` יש סינון לפי פריט; כאן אין שום דבר כזה.
3. **אין עמודת פריט** ברשת.

לכן `engine.addLine`, שמקליד קוד פריט לתוך `#Prt`, הוא הכלי הלא-נכון על המסמך
הזה **גם אחרי שהמיפוי יושלם** — בניגוד לכל מסך אחר שחסר, שבו המיפוי הוא כל מה
שחסר. `addLines` מסרב על הבסיס הזה, לא על בסיס המיפוי.

הצורה המיועדת:

```js
{ payments: [{ method: 'מזומן'|'שיק'|'אשראי', amount, ... }],
  invoices: [{ docNo, amount }] }
```

⚠️ שים לב ש**השיוך לחשבוניות יושב ברשימה ולא בכותרת** — כלומר הוא שלב נפרד
**אחרי** שהקבלה קיימת, לא חלק מיצירתה.

## מסך הכותרת — `KabalaNU.asp`

| שדה | id | הערה |
|---|---|---|
| קבלה | `#DocNo` | ⚠️ לא `#DocId`. ריק ב-ADD; המספר הבא ב-label בסוגריים |
| תאריך · שעה | `#DateDoc` · `#DocTime` | |
| לקוח | `#IdxLk` | |
| **סוג תשלום** | `input[name="Sug"]` | `0`=מזומן · `1`=שיקים · `2`=אשראי. **ברירת מחדל 2** |
| פרטים | `#Pratim` | "קבלה מקופה" ב-7010013 |
| אסמכתא · תאריך | `#Ref` · `#DateIt` | |
| סוכן גביה · סוכן | `#SochenGevya` · `#Sochen` | |
| ניכוי במקור | `#Nikui` · `#wAczNikui` | |
| מטבע · שער | `#Mt` · `#tShaar` · `#DateMt` | |
| קוד תמחירי 1 · 2 | `#Svg` · `#Svg2` | |
| חברת אשראי · סוג עסקה | `#AshraiType` · `#IskaType` | ויזה כאל / תשלומים |
| סכום | `#ScmAshrai` | |
| מספר תשלומים | `#TashNum` · `#ScmTash1` (ראשון) · `#ScmTash` (כל תשלום) | קיימים רק ב-UPDATE |
| **מספר כרטיס · תוקף · CVV · אישור** | `#AshraiNum` · `#AshraiToDate` · `#CvvKab` · `#IshurAshrai` | 🚫 `NEVER_FILL` |
| חשבון קופה | `#getKupa` | |
| פרוט אשראי · פרוט שיקים | `#goAshrai` · `#goLines` | |
| חתימה · הערה | `#getSign` · `#getRemarks` | |
| אישור · ביטול · יציאה | `#OK` · `#Cancel` · `#DoExit` | ⚠️ `#OK` כנראה קולט |

### `Sug` — סוג התשלום. המיפוי מוכח, וברירת המחדל מסוכנת

שלושה רדיו בשם `Sug`, עם `Sug_onclick(0)` / `(1)` / `(2)`.

| ערך | סוג תשלום |
|---|---|
| `0` | מזומן |
| `1` | שיקים |
| `2` | **כרטיס אשראי** |

**איך זה הוכח, בלי לספור פיקסלים:** במצב UPDATE קומקס מנטרל את הרדיו שאינם
מסומנים. על 7010013 — קבלה ששולמה באשראי — ערכים `0` ו-`1` חזרו
`disabled: true` ו-`2` לא. הצילום מסכים. במצב ADD אף אחד מהם לא מנוטרל.

### ⚠️ ברירת המחדל ב-ADD היא **כרטיס אשראי**

הטופס נפתח עם `2` מסומן ובלוק האשראי פעיל. **מי שיקלוט בלי לגעת בשדה — גובה
באשראי.** זו בדיוק המלכודת של `#numScr` ב-`Doc652`, שברירת המחדל שלו היא גם
אשראי. `paymentTypeDefault: '2'` ב-`index.js` קיים כדי שזה יהיה כתוב ולא מופתע.

`#ChkAsh` הוא span עם `ChkAsh_onclick()` ליד אותו label.

### שתי סדרות מספור, ו-`#DocNo` שמשקר בשלוש דרכים

ברשימה יושבות קבלות `70100xx` (האחרונה 7010013, 04/08/2026). אבל טופס ה-ADD
הציע **`(6800005)`** — סדרה אחרת לגמרי. שתי סדרות באותו מסמך, כמו ב-`Doc470`.

**ההשערה:** `70100xx` נוצרות מהקופה, `68000xx` ביד. התמיכה: שדה ה`פרטים` של
7010013 היה **"קבלה מקופה"**. לא אושר — לא נקלטה קבלה.

ולכן שלוש אזהרות על מספר המסמך:

1. **`#DocNo` ריק ב-ADD.** המספר מוקצה בקליטה.
2. **המספר הבא יושב ב-`label` של השדה**, בסוגריים: `(6800005)`.
3. **`DocNo=` ב-query string הוא השורה שהייתה מסומנת ברשימה**, לא המסמך הנוכחי.
   ב-ADD שפתחנו הוא הראה `DocNo=7010013` — הקבלה הקודמת. **לקרוא אותו מה-URL
   נותן את המסמך הלא נכון.**

### שדות שקיימים רק במצב אחד

**רק ב-ADD:** `#CdrDateDoc` (לוח שנה) · `#LkNew` (לקוח חדש) · `#SwHasum`
(צ'קבוקס ליד שורת הלקוח, ייעודו לא ידוע) · `#chg` · וכל בוררי `#CcomboBut*`
(`IdxLk`, `AshraiType`, `IskaType`, `Sochen`, `SochenGevya`, `Svg`, `Svg2`, `Mt`).

⚠️ **`#chg` הוא לא בורר.** לפי תיקון #1 ב-`MAP.md`, בטפסי קומקס הוא **מחליף סוג
לקוח**. הבורר האמיתי הוא `#CcomboBut<שם השדה>`.

**רק ב-UPDATE:** `#TashNum` · `#ScmTash1` · `#ScmTash`. שדות התשלומים מופיעים רק
כשיש עסקת אשראי על המסמך — **היעדרם בטופס ריק הוא טופס ריק, לא קריאה שנכשלה.**

## ✅ הזרימה המלאה — אומתה על 6800005

```
רשימה → #newRec → כותרת (ADD) → #OK
  → KabalaCloseNU (דיאלוג קליטה) → #OK
  → ShiuhAzm (הזמנות, רק אם יש) + ShiuhIdx (חשבוניות) → #OK
  → סגור
```

### סדר המילוי — ואי אפשר לשנות אותו

```js
export const FILL_ORDER = ['customer', 'paymentType', 'amount', 'details'];
```

**בחירת הלקוח מאפסת את סוג התשלום בחזרה לאשראי.** התגלה בהרצה: סימנו מזומן, ואז
הזנו לקוח — `#Mezuman` ירד מהדף ו-`#ScmAshrai` חזר. סכום שיוקלד לפני הלקוח נמחק
יחד עם הבלוק שהחזיק אותו.

⚠️ **תווית הרדיו לא לחיצה.** אין `<label for>`. חייבים ללחוץ על העיגול —
`input[name="Sug"][value="0"]`. לחיצה על המילה "מזומן" לא עושה כלום.

### מזומן — בלוק האשראי יורד מהדף

נוסף `#Mezuman` (סכום). ירדו `#ScmAshrai` · `#AshraiNum` · `#CvvKab` ·
`#IshurAshrai` · `#AshraiType` · `#IskaType` · `#ChkAsh` · `#sFullCard`.

כלומר **בקבלת מזומן אין שדות כרטיס על המסך בכלל**, ו-`NEVER_FILL` רלוונטי
לאשראי בלבד.

### `KabalaCloseNU.asp` — ארבעה כפתורי אישור

| כפתור | id | |
|---|---|---|
| אישור | `#OK` | ← היחיד שהסוכן לוחץ |
| אישור+חדש | `#OKNew` | |
| אישור+שיכפול | `#OKCopy` | |
| **אישור+הפקדה** | `#OKAfk` | 🚫 קולט **ומפקיד לבנק** |

**`#PrintCopies` = `1`.** לא 0 כמו `Doc470`, ולא "לפחות 1" כמו `Doc650`.

### מסכי השיוך — נפתחים לבד

`Kupa/ShiuhIdx/` (חשבוניות) תמיד. `Kupa/ShiuhAzm/` (הזמנות) **רק ללקוח עם
הזמנות פתוחות**.

הכותרת היא **מד ההשלמה**: `סכום קבלה` · `שויך` · `יתרה לשיוך`. **שיוך מלא =
יתרה לשיוך באפס.** לקרוא אותה, לא להניח שהשורות מסתכמות.

`#I0`, `#I1`… הם עמודת ה**שיוך**. ⚠️ **לא מקלידים בהם — לוחצים על הסכום
בעמודת `יתרה לשיוך` (הימנית) והוא קופץ לשמאלית.** זה גם מסביר למה במסך
שנפתח אוטומטית `#I0` כבר מלא: קומקס עושה את ההתאמה הראשונה בעצמו.

כפתורים: `#OK` · `#Cancel` · `#ZeroSh` (איפוס) · `#Izun` (איזון — לא נבדק).

⚠️ **מסך שיוך שנסגר הוא לא אבדה:** ברשימה — לסנן לקבלה, לסמן את השורה, ללחוץ
**"לחשבוניות"** (`#ShiuhHesh`). אומת על 6800007, וזו גם הדרך היחידה לענות
"האם הקבלה שויכה" — לרשת אין עמודה כזאת.

⚠️ **כלל של דרור: לשאול לפני שסוגרים חשבונית.**

> "האם לסגור חשבונית מספר X מתאריך Y?"

ולחכות לתשובה — אלא אם דרור אמר מראש שהשיוך צפוי. המילוי האוטומטי של קומקס הוא
הצעה, לא הוראה.

### ⚠️ קבלה שנקלטה לא מופיעה ברשימה הרגילה

6800005 נקלטה ואומתה — והרשת עדיין הציגה רק `70100xx`. **רק סינון ב-
`#wFindDocNo` הראה אותה.** לכן `verify(ctx, docNo)` מסנן ולא קורא את הרשת, ו-
**"לא ברשימה" הוא לא הוכחה שהקליטה נכשלה.**

זו גם ההוכחה לשתי הסדרות: `70100xx` מהקופה, `68000xx` ביד.

### תצוגת הדפסה

`Kupa/Kab/Kab/Kabala_HtmlP.asp?Doc=<מספר>&PrintCopies=<n>`


## `movesStock: false` — וזה לא אומר בטוח

הקבלה לא מזיזה יחידה אחת. היא רושמת כסף שהתקבל בכרטיס הלקוח. יש `#delRec`
("ביטול") ברשימה, אבל **מה הוא עושה לתנועה בהנהלת חשבונות לא נבדק** — לא להניח
שזה undo.

לכן `create` ו-`finalize` הם **שני שערים בלתי תלויים**. ריכוך של אחד לא מספיק
כדי לקלוט מסמך, ובמסמך הזה זה חשוב במיוחד: כפתור הקליטה יושב על הכותרת, קליק
אחד ממסך שרק נקרא.

## המלכודות של השכנים שתקפות גם כאן

1. **`printCopies` — אסור להעתיק משכן.** `Doc650` דוחה 0 ("חובת הדפסה לפחות
   עותק אחד !") ו-`Doc470` דורש 0. לא ידוע כאן.
2. **`engine.finalize` ידווח "נקלט" על מסמך שלא נקלט** אם דיאלוג הסגירה לא תואם
   ל-`/Close|Kbl|Ishur/i`. מי שיממש קליטה חייב `frames.closeDialog` מפורש.
3. **שמות משתנים בין מסמכים** — `#DocNo` מול `#DocId`, `#OKNew` מול `#OkNew`
   מול `#OKNext`. לקרוא, לא להניח.

## מה כן אפשר היום

**כלום שכותב.** החלופות:

- **מכירה עם פריטים** → `comax-invoice` (Doc650), ממופה ומאומת חי.
- **תשלום על אותו מסמך של המכירה** → `comax-invoice-receipt` (Doc652), מסורב.
- **"מה הלקוח שילם / קנה"** → `comax-history`. קריאה בלבד, עובד.

`#editRec` על קבלה קיימת הוא **קריאה בטוחה** ומוכחת — פותח UPDATE, לא יוצר
כלום, ויוצא ב-`#Cancel`. זה הבסיס לכל שאילתת קריאה עתידית על קבלות.

## הצעד הבא — שהקוד יריץ קבלה בעצמו

הזרימה ידועה במלואה. מה שנשאר הוא **שהקוד כאן יריץ אותה**, ואחריו התת-מסכים:

| מה | מצב |
|---|---|
| **קבלת מזומן מונעת קוד** | ⬅️ הבא בתור. ₪1, עם דרור צופה בכל שלב |
| פרוט שיקים · פרוט אשראי | `#goLines` · `#goAshrai` — לא נפתחו |
| חשבון קופה | `#getKupa` — לא נדרש במזומן |
| `#Izun` · `#delRec` | לא נבדקו |

הרצף שעבד (דרור מריץ את שורת ה-`#newRec`; המסווג חוסם אותה לסוכן):

```bash
npm run open                                    # טרמינל נפרד, מחזיק את המושב
npm run open-program -- a103
node tools/_smoke/click-in-program.mjs "#newRec" receipt-header-add   # ⚠️ קבלה אמיתית
node tools/_smoke/receipt-backout.mjs                                 # #Cancel
npm run close -- all && npm run logoff
```

⚠️ **`receipt-backout` דיווח "המסך עדיין פתוח" בזמן שהוא כבר נסגר.** ה-frame
נעלם רק כמה שניות אחרי הקליק, בדיוק כמו דיאלוג הקליטה ב-`engine.finalize`
שנדרש לו לולאת המתנה. **לאמת מול הצילום או מול רשימת ה-frames לפני שמסיקים
שנשארה קבלה פתוחה.**

⚠️ **היציאה בלי לקלוט: `#Cancel`** (מאומת פעמיים). לא `#OK`.

**אין `receipt-file.mjs`, וזה בכוונה.** כלי שקולט הוא כלי שקולט.

### מה יהפוך את `mapped.header` ל-`true`

לא עוד snapshot. **קבלה אמיתית שמולאה, נקלטה, ואומתה שהיא ברשימה** — ואיתה
התשובה למה `#OK` באמת עושה. זו החלטה נפרדת של דרור.

## אחרי שהמיפוי יושלם — מה עוד צריך להשתנות

`registry.chain` מעביר לכל שלב `step.items` וקורא ל-`agent.addLines(ctx, items)`.
**קבלה תצטרך `step.payments`.** השינוי לא נעשה עכשיו בכוונה — קוד שאי אפשר
להריץ הוא קוד מת, ו-`assertReady` ממילא עוצר שלב קבלה בשרשרת לפני שהמסמך
הראשון מתחיל.

השרשרת שמחכה לזה: **חשבונית מס → קבלה**, כפעולה אחת.

⚠️ **`a146` — "קבלות לעו"ש" — הוא מסמך אחר** (הפקדה לחשבון עובר-ושב), מחוץ
להיקף. `#AfKadot` ("איתור הפקדות") והעמודה `הופקד` ברשימה הן החוט אליו: ההפקדה
היא שלב נפרד **אחרי** הקבלה. אל תמפה אותו במקום `a103`.

`mapped: true` הוא הצהרה שהזרימה רצה מקצה לקצה על מסמך אמיתי — לא ש-snapshot
צולם. הממצאים נכתבים ל-`MAP.md`, לקובץ הזה ול-`.claude/agents/comax-receipt.md`
**באותו commit**.

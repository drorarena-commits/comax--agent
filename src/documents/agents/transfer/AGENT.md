# סוכן תעודת העברה — `Doc470` (`a111`)

**מופה במלואו והורץ מקצה לקצה ב-02/09/2026.** המיפוי נעשה על טיוטה שנוטשה, והקליטה
אומתה על **4700239** — ראשי → מחסן קבוצות, 24 יחידות, 3,448.80, התעודה שמאזנת את
חשבונית 6500084 לשחייני על.

## למה הוא החריג שמצדיק ארכיטקטורת סוכן-למסמך

לתעודת העברה **אין לקוח, אין מחירון ואין מע"מ**. הכותרת שלה היא שני מחסנים —
`#Store` (ממחסן) ו-`#Store1` (למחסן) — והשורות הן פריט + כמות. כל מה שהמסמכים
המכירתיים מתעסקים בו פשוט לא קיים כאן: מסך השורות מצהיר `לפי מחירון: לא נבחר`,
המחיר בשורה נכנס 0.00, ובסיכום אין `#Scm_Maam` ואין `#ScmBeforeMaam` בכלל.

ולעומת זאת שני הדברים שכן חשובים לה — **שהמקור באמת מחזיק את הסחורה**, ו**שהחץ
מצביע לכיוון שביקשו** — אף אחד ממסמכי המכירה לא בודק. אז הסוכן מחליף את השערים
במקום לוותר עליהם. **כיוון הוא לתעודת העברה מה שמשטר המע"מ הוא לחשבונית.**

## המפה

| מסך | frame | מה יש בו |
|---|---|---|
| רשימה | `Doc470V.asp` | `#newRec` · `#wFindDocNo` · `#wFindLkNmFrom` (ממחסן) · `#wFindLkNm` (למחסן) · `#wFindRemarks` · `#wFindDate` |
| כותרת | `Doc470U.asp` | `#Store` · `#Store1` · `#DateDoc` · `#Pratim` · `#Ref`/`#RefA` · `#DocId` · `#OK` · `#OKNot` · `#Cancel` |
| שורות | `Doc470LinesV.asp` | `#newRec` · `#ScmBeforeDis` (סכום) · `#Scm_Cmt` (**כמות**) · `#wrkStore`/`#wrkStoreTo` · `#OK` = **קליטה** |
| שורה | `Doc470LinesU.asp` | `#Prt` · `#Cmt` · `#Mhr` · `#AczDis` · `#Remark` · `#Siba` · `#OK` · `#OkNew` · `#OkCopy` |

הנתיב: `Erp/Mlay/TeydatAv/Doc/` — מתחת ל-`Mlay`, לא מתחת ל-`Mehirot` כמו מסמכי
המכירה. אותו דפוס `V/U/LinesV/LinesU` בכל זאת, ולכן המנוע המשותף מניע אותו כמות
שהוא.

## שבעה דברים שאי אפשר לגלות מקריאת הקוד

1. **`#OkNew` ב-k קטנה** — האיות של Doc612, לא `#OKNew` של Doc650. יש כאן גם וי
   רביעי שאין במסמכי המכירה: `#OkCopy` (אישור+שיכפול).

2. **`#Siba` (סיבת העברה) ריק במערכת הזאת** — הבורר עונה "אין נתונים". לא כותבים
   אליו כלום.

3. **`#Siba` קיים גם ב-`Doc470V` ומוסתר שם.** `npm run options -- Siba` נופל על
   ה-frame הלא נכון בגלל זה ("resolved to hidden"). לקרוא בוררים של מסך השורות
   מול ה-frame במפורש.

4. **הסיכום הוא כמות, לא כסף.** `#Scm_Cmt` הוא המספר שקובע כאן; `#ScmBeforeDis`
   נשאר ריק כשהמחירים 0. שער "יש מה לקלוט" נשען על הכמות.

5. **`#OK` ברשת לא קיים ברשת ריקה.** מופיע רק אחרי שנכנסה שורה, ואז מתויג
   `(Alt+e) קליטת תעודת העברה`. הופעתו היא בעצמה ההוכחה שהשורה נחתה.

6. **מספרי התעודות הם `470xxxx`, ובאותה רשימה יושבת סדרה ישנה `601xxxx`.** ברירת
   המחדל של הרשימה היא מיון לפי מספר יורד, ולכן 6010294 (31/08) צף מעל 4700238
   (01/09) והעמוד הראשון נראה בן שבוע. **לסנן ב-`#wFindDocNo`, לא לסרוק.**

7. **היציאה מהשורות מרימה `confirm` של הדפדפן** — "האם ברצונך לצאת ללא שמירה?".
   `browser.js` מאשר אותו אוטומטית ורושם ביומן.

## הכיוון

`assertDirection` קורא את שני המחסנים **מהמסמך שנקלט**, לא מהטופס שהרגע הקלידו
אליו: מסך השורות מדפיס אותם ב-`#wrkStore`/`#wrkStoreKod` ו-`#wrkStoreTo`/
`#wrkStoreKodTo`. הוא מסרב על אי-התאמה למה שהתבקש, ועל `ממחסן == למחסן`.

## מלאי המקור

הבדיקה היא מ-`content/מלאי-מלא-*.csv` המקומי — לא מדוח חדש בקומקס (כלל של דרור).

⚠️ **הייצוא לא מכסה את כל המחסנים.** העמודות שלו הן ראשי · מכולה וינגייט · רמת גן ·
ספורט & מור רמלה · WIX. **מחסן קבוצות (11) — היעד של ההעברה שבשבילה הסוכן קיים —
לא ביניהם.** לכן:

| מצב | תשובה |
|---|---|
| המקור מכוסה ויש מספיק | עובר |
| המקור מכוסה ואין מספיק | **סירוב** |
| הפריט לא בייצוא בכלל | **סירוב** — "לא ידוע" הוא לא "בסדר" |
| המקור לא עמודה בייצוא (קבוצות) | **סירוב** |

`allowShort: true` היא הדרך היחידה לעבור, והיא אמירה מפורשת של הקורא. קומקס עצמה
מתירה מלאי שלילי (בייצוא יש `-529`), ולכן זו החלטה ולא תקלה.

הקוד מגיע מקומקס כ-`"3468337082118 - משקפת קוברה..."` אחרי שהחיפוש נפתר, ולכן
הבדיקה חותכת ב-`" - "` הראשון. הגרסה הראשונה של השער ניקדה שורה לא-ידועה כ"לא
חסרה" והייתה מעבירה בדיוק את המקרה שבשבילו הוא קיים.

## התהליך שדרור עובד לפיו

מוציאים חשבונית **ממחסן קבוצות (11)**, ואז מעבירים מלאי מ**ראשי (1)** לקבוצות
כדי לאפס את היתרה שנוצרה. זו הסיבה שסוכן־העל יודע לשרשר: `חשבונית → העברה`
בפעולה אחת, עם עצירה בכישלון הראשון.

התעודה 4700237 (02/09/2026) היא הדוגמה החיה: ראשי → מחסן קבוצות, 6 יחידות,
ובשדה פרטים `חשבונית 6500083 -`. **כדאי לרשום שם את מספר החשבונית** — זה מה
שהופך את הזוג לקריא חודש אחרי.

הכיוון הזה גם נופל בדיוק בצד הנוח של שער המלאי: המקור הוא ראשי, שכן מכוסה בייצוא.
העברה **מ**קבוצות תיעצר, ובצדק.

## קודי מחסנים

`knowledge/lists.json` — ראשי (1) · רמת גן (3) · מכירות אינטרנט (4) · נגרר (5) ·
קאנטרי אביבים (6) · קאנטרי עפולה (7) · מחסן אונליין (10) · **מחסן קבוצות (11)** ·
מרום נווה-פליקס (12) · ספורט & מור רמלה (13) · הדפסות כדור מים (14) ·
מכולה וינגייט (15) · WIX (16) · פגומים/לבדיקה (17).

## הקליטה — שתי לחיצות, בסדר הזה

```
1. החץ הירוק בפינה השמאלית העליונה של הרשת   #OK  "(Alt+e) קליטת תעודת העברה"
      ↓  נפתח Doc470CloseU.asp — "אישור קליטת תעודת העברה"
2. הווי הירוק בדיאלוג                          #OK  (עותקים כבר 0)
```

זה הסדר שדרור הראה, וזה מה שרץ ב-4700239. אחרי הקליטה נשאר `Doc470CloseIo_sql.asp`,
בדיוק כמו `Doc650CloseIo_sql.asp` בחשבונית — ולכן `closeDialog` ננעל על
`Doc470CloseU.asp` בסוף המחרוזת, ושתי המסגרות לא מתבלבלות.

**עותקים = 0 תמיד** — כלל של דרור. זה מסמך מלאי פנימי; אין למי למסור עותק מודפס.
בחשבונית זה הפוך (0 נדחה שם) — אל תעתיק ערכים בין המסמכים.

### ⚠️ המלכודת שעלתה בקליטה הראשונה

הכותרת `Doc470U.asp` נושאת `SwNoClose=0` בשורת השאילתה. ברירת המחדל של המנוע
לדיאלוג הסגירה היא `/Close|Kbl|Ishur/i`, והיא התאימה **לכותרת**: `finalize` חיפש
שם `#PrintCopies` ולא מצא, לחץ על `#OK` של הכותרת, ואז דיווח "המסמך לא נקלט" בזמן
שדיאלוג הקליטה האמיתי עמד פתוח על המסך.

התיקון בשורש: `frameFor` במנוע מתאים עכשיו על **ה-path בלבד**, בדיוק הכלל שכבר
תועד על `Doc650_ShihzurP` שתפס את `Doc650_HtmlP_T13.asp`. אותה משפחת באגים, והפעם
היא עלתה בלחיצה על הכפתור הלא נכון במסמך שמזיז מלאי.

## מה עוד לא נבדק

- **מה קורה כשאין מספיק מלאי במקור** — הסוכן עוצר לפני זה, אז לא ראינו את התגובה
  של קומקס.
- `#ImpExcel` / `#FromList` / `#CopyPrt` (העתקת פריטים ממסמך אחר) — קיימים ברשת
  ולא נגענו בהם. `#CopyPrt` הוא ככל הנראה הדרך הקצרה להעברה שמאזנת חשבונית.

## בדיקה

```bash
node tools/_smoke/stock-gate.mjs
```

תשעה תרחישים של שער המלאי והכיוון, בלי דפדפן. להריץ אחרי כל שינוי ב-
`checkSourceStock` או ב-`finalize`.

```bash
npm run open-program -- a111
node tools/_smoke/click-in-program.mjs "#newRec"
node tools/_smoke/transfer-header-drive.mjs <input.json>   # כותרת → מסך שורות
node tools/_smoke/transfer-add-lines.mjs    <input.json>   # שורות + כל השערים, בלי קליטה
node tools/_smoke/transfer-file.mjs         <input.json>   # ⚠️ קולט
```

**שלושה כלים ולא שניים, וזה לא סגנון.** ל-`transfer-add-lines` אין `--confirm`
בכוונה: הפעולה הראשונה שלו היא להוסיף שורות, אז דגל קליטה עליו מריץ את `addLines`
שוב על מסמך שכבר יש לו אותן. בדיוק זה קרה ב-4700239 — "תקלוט" לחץ `#newRec` והתחיל
שורה 5. כלי אחד מוסיף, כלי אחד קולט.

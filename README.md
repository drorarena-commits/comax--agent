# סוכן קומקס

סוכן שמפעיל את פורטל קומקס דרך דפדפן אמיתי על המחשב, בקצב אנושי, בלי API.

## איך זה עובד

Chrome האמיתי שמותקן במחשב (לא Chromium, לא headless) רץ עם **פרופיל ייעודי** בתיקייה
`.chrome-profile/`. מתחברים לקומקס ידנית פעם אחת — הסשן נשמר בפרופיל, והסוכן ממשיך משם.
החלון נראה לעין, אז אפשר לראות כל פעולה שהסוכן עושה.

## פקודות

```bash
npm run open                      # פותח את חלון הסוכן ומשאיר אותו פתוח (מצב לימוד)
npm run logoff                    # התנתקות מסודרת — משחררת את הסשן בשרת
npm run hold -- 45                # מחזיק את המושב 45 דקות של עבודה ידנית (off / status)
npm run snapshot -- <שם-מסך>      # ממפה את המסך שפתוח כרגע -> knowledge/screens/
npm run run -- --list             # מציג את המשימות הקיימות
npm run run -- <משימה> --json '{...}'            # הרצה יבשה (עוצר לפני שמירה)
npm run run -- <משימה> --json '{...}' --confirm  # הרצה אמיתית
npm run run -- stock-matrix        # מטריצת מחסנים -> data/exports/
npm run run -- items-export        # קטלוג הפריטים -> data/exports/ (רזה: רק עמודות עם תוכן)
npm run run -- customer-history --json '{"customer":"112447","item":"שנורקל"}'
npm run run -- quote-read --json '{"customer":"בני הרצליה"}'          # רשימת ההצעות של לקוח
npm run run -- quote-read --json '{"docNo":"6120029","customer":"בני הרצליה"}'  # וקריאת שורותיה
npm run run -- invoice-import-lines --json '{"customer":"112474","store":"WIX","priceList":"מכירה ראשי","file":"...xlsx"}'  # חשבונית גדולה: שורות מאקסל
npm run run -- invoice-finalize --json '{"customer":"112474","expectDoc":"6500089","expectTotal":"22497","store":"WIX","priceList":"מכירה ראשי"}'  # קליטה אחרי אישור
npm run run -- invoice-reset --json '{"customer":"112001"}'   # מוחק את כל שורות הטיוטה
npm run payroll -- --to <כתובת>    # דוח נוכחות חודשי לשכר -> מייל מקומקס
npm run mail-send -- --to <כתובת> --subject "..." --body-file b.txt --attach f.pdf  # מייל עם קבצים (יבש)
npm run mail-send -- ... --confirm                    # ורק זה שולח בפועל
npm run mail-get -- <messageId> <חלק-משם-הקובץ>       # מוריד קובץ מצורף ממייל לדיסק
npm run items-file                 # בונה את קובץ ההקמה + מריץ את שערי האימות
npm run items-import -- --json '{"file":"data/exports/הקמה-קומקס-175.xlsx"}'  # יבוא פריטים (יבש)
npm run cost-file -- --import      # מחירי עלות מדוח רכישות -> שני קבצי CSV
npm run run -- cost-import --json '{"file":"...csv","list":9}'   # יבוא מחירון (יבש)
```

`customer-history` עונה על "מה הלקוח הזה קנה בעבר, ובאיזה מחיר". הוא סורק **בדיוק
שלושה מסמכים** — חשבונית מס (`a157`), חשבונית מס/קבלה (`a132`) והצעת מחיר (`a164`) —
ולא תעודות משלוח או כל דבר אחר. עם `item` הוא מסנן לשורות התואמות וממיין מהישן לחדש,
כך שרואים אם המחיר חזר על עצמו. משימת קריאה בלבד — רצה בלי `--confirm`.


`stock-matrix` מריץ את דוח יתרות המלאי. בכל הרצה הוא קובע את התאריך להיום ואת
המחסנים לפי `reports.stockMatrix.warehouses` בקונפיג, **קורא את השדות בחזרה
מהמסך ומשווה**, ועוצר בלי ללחוץ אישור אם משהו לא תואם. לדריסה חד-פעמית:
`--json '{"warehouses":["1","3"],"date":"31/08/2026"}'`.

## קריאת הצעת מחיר קיימת — `quote-read`

```bash
npm run run -- quote-read --json '{"customer":"בני הרצליה"}'
npm run run -- quote-read --json '{"docNo":"6120029","customer":"בני הרצליה"}'
npm run run -- quote-read --json '{"customer":"בני הרצליה","item":"backpack"}'
npm run run -- quote-read --json '{"docNo":"6120029","year":2026}'   # שנה כמכריע כפילות
```

קריאה בלבד (`writes: false`) — פותחת את המסמך, קוראת את **כל** שורותיו ויוצאת
ב-`#DoExit` → `#Cancel`, לעולם לא `#OK`. רשימה בלבד עולה כ-50 שניות; כל מסמך
שנפתח מוסיף כ-25.

שני דברים שהיא אוכפת, ושניהם נמדדו חי:

- **מספר מסמך אינו ייחודי.** המספור הרץ מתאפס בכל שנה, ולכן 6120029 קיים גם
  לעמותת בני הרצליה (20/05/2026, 16,284) וגם לעיריית רמלה (16/07/2025, 531).
  המסמך נפתח לפי **השורה** ברשת, ומספר בלי `customer` או `year` שמכריע גורם
  לעצירה — לא לבחירה בראשונה.
  ⚠️ ורשימת ההצעות (`a164`) מציגה מסמכים משנה קודמת גם כששנת העבודה היא 2026,
  בעוד שחשבונית באותו גיל דורשת **החלפת חברה** ב-`a157`. אין להסיק את
  ההתנהגות של מסך אחד ממסך אחר.
- **הוכחת שלמות (כלל 16).** הרשת מחולקת ל-10 שורות בעמוד, ועמוד ראשון נראה כמו
  מסמך שלם. [`src/documents/read-lines.js`](src/documents/read-lines.js) מדפדף
  ומוכיח את מה שקרא מול סיכום המסמך — `סה"כ כמות` בתעודת העברה, וסכום השורות
  מול `סה"כ` בהצעת מחיר, שאין בה סיכום כמות בכלל.

## סשן אחד בלבד

לקוד המשתמש בקומקס יש מושב אחד. סגירת החלון **לא** משחררת אותו — השרת ממשיך
להחזיק את הסשן כמה דקות, וההתחברות הבאה נדחית עם "קוד משתמש בשימוש". לכן:

- `npm run open` מתנתק מסודר (`#LOGOFF_Prog`) לפני שהוא סוגר את החלון ב-Ctrl+C.
- `npm run open` מתחבר לחלון שכבר פתוח במקום לפתוח שני ולהתנגש עם עצמו.
- אם החלון נהרג בלי התנתקות — `npm run logoff` משחרר, ואם אין חלון פתוח פשוט
  להמתין כ-3 דקות.
- התחברות שנדחתה עם "קוד משתמש בשימוש" מנסה שוב לבד
  (`login.busyRetries` / `login.busyWaitMs` בקונפיג), במקום להיכשל.

## `npm run ensure` — הפעלה בלי טרמינל פתוח

`npm run open` הוא **מצב לימוד**: הוא מחזיק את החלון ומחכה, וצריך טרמינל שנשאר
פתוח. זה בסדר כשיושבים מול המחשב, וחסר תועלת כשמפעילים את הסוכן מהאייפון.

```
npm run ensure          מוודא שהכל מוכן, ומדפיס READY / NEEDS_LOGIN / FAILED
npm run ensure -- --reset   מנקה חסימת לוגין אחרי תיקון .env
```

מה הוא עושה: בודק את פורט 9222, **משגר כרום מנותק** (detached) אם הוא מת — כלומר
חלון ששורד את סיום התהליך — מחכה שהפורט יעלה, ומוודא שהסשן חי. אם לא, מתחבר
מ-`.env`. `tools/run.js` קורא לו בעצמו לפני כל משימה, אז בדרך כלל לא צריך להריץ
אותו ידנית.

**שחרור המושב — `npm run idle-logoff`.** אין יותר `logoff` בסוף כל משימה: הסשן
נשאר פתוח כדי שבקשות רצופות ירוצו מיד בלי לוגין חוזר. אבל קומקס **לא** ישחרר
לבד — חלון פתוח מתשאל את השרת כל ~90 שניות ומחזיק את הסשן חי, כך שהקוד יישאר
תפוס כל היום וכל עמית יקבל "קוד משתמש בשימוש".

`npm run idle-logoff` מתנתק רק כששני התנאים מתקיימים: אין הרצה פעילה
(`runs/.lock`), ועברו 10 דקות רצופות בלי שימוש (`runs/.last-activity`, נכתב
בתחילת כל הרצה **וגם** בסופה). מיועד למשימה מתוזמנת שרצה כל כמה דקות — יוצא
בשקט כשאין מה לעשות. הסף ניתן לשינוי ב-`COMAX_IDLE_MINUTES`.

**הרצה אחת בכל רגע.** `runs/.lock` נלקח בתחילת כל משימה ומשוחרר בסופה, כולל
`npm run logoff`. הרצה שנייה מסרבת ואומרת מי מחזיק. זה מה שמונע מהרצה מהטלפון
להיכנס על הרצה מקומית — ולהפך.

`npm run open` עדיין שימושי למיפוי ולעבודה ידנית מול החלון; שאר הכלים מתחברים
לאותו חלון דרך פורט 9222.


## הקטלוג — `npm run catalog-csv`

`data/catalog/items.json` הוא טבלת התרגום מקוד/ברקוד למק"ט חלופי, דגם וצבע.
בלעדיו כל דוח מציג ברקודים גולמיים, בניגוד לכלל 5 — והוא **חסר בכל מחשב חדש**,
כי `data/` לא נשמר ב-git.

```
npm run catalog-csv            בונה מהייצוא החדש ב-content/
npm run catalog-csv -- <קובץ>  מקובץ מסוים
```

שניות, בלי לגעת בקומקס ובלי לתפוס את המושב. הייצוא ש-`content/` מחזיק הוא CSV
שכולל `מק"ט חלופי` ו`ברקוד`, ו-`content/` **כן** נשמר ב-git — כלומר הקטלוג
ניתן לשחזור בכל מחשב שמושך את הרפו.

⚠️ זה **לא** אותו מסלול כמו `npm run catalog -- build items`, שמצפה לייצוא
**HTML** של קומקס ולכן מחייב ייצוא טרי של כחצי שעה שתופס את המושב.

## מבנה

```
config/comax.config.json   כתובת הפורטל, טווחי השהיה, timeouts
src/human.js               קצב אנושי — כל פעולה עוברת דרכו
src/browser.js             הפעלת Chrome / התחברות לחלון קיים
src/inspect.js             מנוע המיפוי (frames, תוויות עברית, סלקטורים)
src/logger.js              לוג צעדים + צילומי מסך ל-runs/
src/tasks/                 משימה אחת = קובץ אחד
tools/open.js              מצב לימוד
tools/snapshot.js          מיפוי מסך
tools/run.js               דיספצ'ר משימות
.claude/agents/            סוכני Claude — comax-items, comax-invoice, comax-quote…
src/items/                 בניית קובץ ההקמה + שער הדגמים והצבעים
knowledge/MAP.md           המפה של המערכת
knowledge/screens/         מתכון סלקטורים לכל מסך
runs/                      לוג וצילומי מסך לכל הרצה
```

## בטיחות

- כל משימה **כותבת** רצה כברירת מחדל ב־dry-run: ממלאת את הטופס, מצלמת, ועוצרת לפני
  כפתור השמירה. רק `--confirm` עובר את הקו הזה.
- משימות **קריאה בלבד** (דוחות) מסומנות `writes: false` ורצות חופשי.
- כל הרצה נשמרת ב־`runs/<timestamp>-<משימה>/` עם `steps.log` וצילומי מסך.

## קצב אנושי

`src/human.js` אוכף רצפה של 2 שניות בין כל שתי פעולות (`pace.minGapMs`), משהה
2–3.5 שניות "מחשבה" לפני פעולות משמעותיות, ומרחף עם העכבר לפני כל קליק. כל
הערכים בקובץ הקונפיג.

שני דברים שנראים כמו קצב אנושי ואינם, ולכן הוסרו:

- **הקלדה תו-תו** — מאז 09/09/2026 ברירת המחדל היא הדבקה (`insertText`), עם
  קריאה חוזרת והשוואה; אי-התאמה נופלת חזרה להקלדה אמיתית. שדה שקומקס לא צריך
  לשלוף עבורו נתון (`free: true`) גם מדלג על שער שתי השניות.
- **תזוזת עכבר בכמה צעדים** — `steps` ירד ל-1–2 ב-10/09/2026. Max2000 הוא
  frameset, ו-Playwright שולח כל mousemove ביניים לכל ה-frames (61 בעת המדידה),
  כך שכל step עלה **~שנייה שלמה**. זה קיצר את זמן הקמת החשבונית ב-**57%**
  (100.9s ⇒ 43.4s) בלי לגעת ב-`minGapMs` או ב-`think`. הפירוט המלא ב-
  [`knowledge/MAP.md`](knowledge/MAP.md).

⛔ אין להעלות את `steps` בחזרה "כדי שייראה אנושי" — האנושיות יושבת ב-`gate`
וב-`think`, לא באינטרפולציה של העכבר.

### למה סגירת חלון לא משחררת

בקשת השחרור היא `Max2000_NET_2022/Server/CloseSession.ashx`, שקומקס שולחת
מתוך `top.onUnload('fset')` — **אסינכרונית, בתוך unload**. הדפדפן הורג בקשות
כאלה כשהחלון נסגר, ולכן היא כמעט אף פעם לא יוצאת. הכפתור התנתקות בסרגל
(`#LOGOFF_Prog`) לא עוזר: כל מה שהוא עושה זה `top.window.close()`, ש-Chrome
חוסם ושלא שולח לשרת כלום.
לכן `logoff()` קורא ל-`onUnload` בעצמו **ומחכה לתשובה**.

### נתונים מקומיים אחרי כתיבה

`data/exports/*.html` הוא צילום מצב ולא נערך לעולם. כל תנועת מלאי שהסוכן מבצע
נרשמת ב-`data/movements.jsonl`, ו-`npm run merge` מחיל את התנועות שנרשמו אחרי
צילום הדוח. כך התמונה המקומית תמיד מעודכנת בלי לזייף את מה שקומקס באמת החזירה.

### דוח מלאי מלא

הדוח של קומקס נעצר ב-1500 שורות, והמגבלה היא על שורות פריטים ולא על מחסנים.
`npm run full-stock` מריץ אותו ב-11 מנות לפי טווחי קוד פריט ומאחד — 12,476
פריטים, כ-25 דקות. `npm run merge-full` מצליב עם הקטלוג ל-`content/`.

## הקטלוג המלא — `items-export`

```bash
npm run run -- items-export                             # ברירת מחדל רזה — 37 עמודות
npm run run -- items-export --json '{"fields":"all"}'   # כל 97 העמודות (58 MB)
npm run run -- items-export --json '{"probe":true}'     # מיפוי הדיאלוג בלבד
```

מסלול אחר מדוח המטריצה: מסך **פריטים** (a84) → לשונית **נוספים** → **יצוא
לאקסל**. אין תקרת 1500 שורות — **כל 13,107 הפריטים בהרצה אחת של פחות משתי
דקות**. ברירת המחדל היא **37 עמודות שנושאות מידע** — ברקוד, קוד חלופי,
מחלקה, קבוצה, ספק, דגם, צבע, מותג, מחיר, עלות. קריאה בלבד, לא נוגע במלאי.

הרשימה הרזה נמדדה: מתוך 101 העמודות של `fields:"all"` היו 45 ריקות בכל
13,107 השורות, שלוש קבועות ושלוש כפילויות מדויקות — 58 MB מול 22 MB.

שלושה דברים שהמשימה אוכפת ואי אפשר לנחש: תיבות בחירת העמודות אינן משפיעות
עד ש-`GetMiunVal()` רץ, תחומי הסינון נשארים דביקים מהרצה קודמת (נמצא מחסן 15
שנשאר מייצוא אחר), והקובץ שיורד הוא HTML ב-windows-1255 בתחפושת `.xls` — 58
MB שלא נפתח באייפון, ולכן הוא מומר ל-CSV ב-UTF-8.
כל מנה נשמרת בנפרד, ואפשר להמשיך אחרי עצירה עם `--from N`.

## הקמת פריטים — הסוכן `comax-items`

**הזרימה כולה יושבת אצל סוכן Claude ייעודי: [`comax-items`](.claude/agents/comax-items.md).**
הוא מלווה פריט חדש מבניית הקובץ ועד שהוא קיים בקומקס, ומחזיק את המלכודות
שנשכחות בדיוק כשהן חשובות. אליו מנתבים "תקים את הפריטים החדשים" · "תעלה את
קובץ ההקמה" · "תייבא פריטים מאקסל" · "תקים צבע/דגם".

| שלב | הפקודה |
|---|---|
| 1. מקורות | `npm run catalogs-sync` |
| 2. בניית הקובץ + שערי האימות | `npm run items-file` |
| 3. הקמת דגמים וצבעים חסרים | `color-create` (a125) · `model-create` (a77) |
| 4. היבוא לקומקס | `npm run items-import` |
| 5. אימות אחרי | `npm run run -- items-export` והצלבה מקומית |

✅ **הורץ חי 07/09/2026 — 175 פריטי ארנה הוקמו**, והאימות היה 13,282 פריטים
בקטלוג מול 13,107 לפני (הפרש 175 בדיוק) עם התאמה מלאה ב-13 השדות הממופים.

### בניית הקובץ — `items-import-file`

```bash
node tools/items-import-file.js                     # בונה את קובץ ההקמה ומריץ את כל השערים
node tools/items-import-file.js יעד.xlsx            # ליעד אחר
node tools/items-import-file.js יעד.xlsx --comax קטלוג-ישן.csv   # לשחזר מנה שכבר נקלטה
```

⛔ **הכלי מסרב לדרוס קובץ קיים בתוצאה ריקה.** אחרי שמנה נקלטת הפריטים שלה
מדולגים כ"כבר קיימים בקומקס", וריצה חוזרת הייתה מוחקת את הקובץ בשקט — זה קרה.
`--comax` הוא הדרך לבנות מחדש מנה שכבר נקלטה, מול קטלוג מלפני הקליטה.

מרכיב את שורות ההקמה משלושת המקורות (הקטלוג של קומקס · כרטיס הפריט של ספורט
אנד מור · קטלוג ארנה) לחוברת בת שלושה גיליונות: `להקמה` · `דורש החלטה` ·
`מאסטר חסר`. קריאה בלבד, לא נוגע בקומקס.

⛔ **שער המאסטר חוסם.** בקומקס `דגם` ו`צבע` הם ישויות נפרדות מהפריט, ופריט
שנקלט עם קוד שלא הוקם ייצור ערך יתום. השער בודק כל קוד מול הקטלוג ונכשל
(`exit 1`) אם משהו חסר, ומסמן בצהוב קוד שיש לו **דומה ולא זהה** בקומקס — מסד
הצבעים מחזיק `75` ו-`075` כשתי רשומות שונות. הצבע עצמו נכתב **כפי שהוא מגיע
מהמקור, בלי ריפוד**.

### היבוא עצמו — `items-import`

```bash
npm run items-import -- --json '{"probe":true}'                       # מיפוי הדיאלוג בלבד
npm run items-import -- --json '{"file":"data/exports/הקמה-קומקס-175.xlsx"}'            # יבש
npm run items-import -- --json '{"file":"data/exports/הקמה-קומקס-175.xlsx"}' --confirm  # קולט
npm run items-import -- --json '{"file":"...xlsx","previewOnly":true}' --confirm  # מסך התוצאה בלבד — לא קולט
```

`a84` → לשונית **"נוספים"** → **`#ImpExl`**, סוג יבוא `0 · פריטים`. המשימה
ממלאת את מיפוי העמודות, בוחרת את הקובץ, מצלמת ו**עוצרת לפני `#ok`** — הקליטה
בלתי הפיכה. שלושה שערים חוסמים לפניה: **שער המאסטר** (דגם וצבע קיימים בקומקס),
**שער הסוג** (כתובת `FMiun` מאשרת שנטענה רשימת השדות של "פריטים"), ו**שער
העמודות**.

⚠️ **שער העמודות אינו קישוט.** בכל שדה במסך, `.value` היא אות העמודה שרואים
ו-`.title` הוא המספר הסידורי — ו**מה שנשלח לשרת הוא ה-`title`**. אפשר לראות
`M` ליד "מחיר מכירה" ולשלוח עמודה אחרת. ההוכחה שהשער תופס היא
`node tools/_smoke/items-import-gate.mjs`, שמקלקל שדה בכוונה. הפירוט:
`knowledge/MAP.md`, פרק "יבוא פריטים מאקסל".

תיבות ההתנהגות **נשארות כפי שקומקס טעון אותן** — המשימה קוראת ומדווחת, ועוצרת
רק אם `הקמת פריט` אינו "בלבד" או ש`פריט חדש` אינו מסומן.

### נתוני אתר — הדגל שבלעדיו פריט לא מסונכרן עם האתר

```bash
npm run site-file                                        # בונה את הקובץ ל-10 כובעי האתר
npm run site-file -- יעד.xlsx 91662055 91662071          # מק"טים מפורשים
npm run items-import -- --json '{"file":"...xlsx","impType":"3"}'            # יבש
npm run items-import -- --json '{"file":"...xlsx","impType":"3"}' --confirm  # קולט
```

פריט בקומקס מסונכרן עם אתר ארנה **רק אם הסימון "לא להציג" במסך נתוני אתר
מבוטל**. פריט חדש נוצר עם הדגל **מסומן**, ולכן הוא מוקם נכון, מקבל מק"ט תואם
ומלאי — **ופשוט לא מופיע**. הדגל מיוצא (עמודה `נתוני אתר - לא להציג`), כך
שאפשר למדוד מי חסום מ-`content/` בלי לגעת בקומקס.

הביטול בבאצ' הוא **סוג יבוא 3 — "נתוני אתר לפריט"** באותו מסך יבוא. לסוג הזה
מסכי תוצאה, מיפוי עמודות ונוסח מונה **משלו** — הכול ב-`TYPES` שב-
[`items-import.js`](src/tasks/items-import.js) ובפרק שב-
[`knowledge/items-setup.md`](knowledge/items-setup.md).

### מחירי עלות — `cost-file` ו-`cost-import`

**מסלול נפרד לגמרי מיבוא הפריטים.** הוא לא עובר ב-`a84` אלא ב-`a6`
"מחירונים" → כפתור **"יבוא מאקסל"**, והוא **דורש CSV** ולא xlsx.

```bash
npm run cost-file                            # הדוח לעיון — 3 גיליונות, בלי לגעת בקומקס
npm run cost-file -- --import                # שני קבצי היבוא
npm run cost-file -- --only last --pilot 5   # קובץ פיילוט
npm run run -- cost-import --json '{"probe":true}'                        # מיפוי הדיאלוג
npm run run -- cost-import --json '{"file":"...csv","list":9}'            # יבש
npm run run -- cost-import --json '{"file":"...csv","list":9}' --confirm  # קולט
```

הקובץ הוא **שלוש עמודות: `מחירון,ברקוד,מחיר`**, ו**העמודה הראשונה היא מספר
המחירון — לא קוד הספק**. אצל רוב הספקים הם זהים במקרה, ודווקא אצל ספורט אנד
מור הם שונים (ספק `121012`, מחירון `1210012`). הרשימה המלאה ב-`PRICE_LISTS`
שב-`src/items/build-cost.js`, ומקורה במסך `a6` עצמו.

שתי הרצות, שני תפקידים: **מחירון הספק** מקבל את הממוצע המשוקלל לפי הכמות,
ו**מחיר קניה אחרונה (מחירון 9)** מקבל את הרכישה האחרונה בפועל.

⚠️ **הקליטה היא `#OK` פעמיים** — הראשון בונה תצוגה מקדימה, השני קולט. ו-
`#button1` בטופס הוא בכלל כפתור העזרה. השער ממתין למונה `סה"כ פריטים: N מתוך
M` **עד ש-N שווה ל-M**, כי רשת דחיות שעוד לא נטענה נראית בדיוק כמו אפס דחיות.
הפירוט: `knowledge/MAP.md`, פרק "מחירונים — a6".

✅ מאומת בהרצה חיה (07/09/2026): 719 שורות למחירון 9, 0 דחיות, 225 שינו ערך.

### קטלוגי ארנה — `catalogs-sync`

```bash
npm run catalogs-sync -- <manifest.json>          # מוריד מדרייב, ממיר, ומדווח כיסוי
npm run catalogs-sync -- --state                   # מה כבר מסונכרן
```

תיאור הצבע (`שם פריט באנגלית`) מגיע מקבצים שארנה ואליסה שולחות. הם יושבים
ב-**`content/arena-catalogs/`** ולכן **נשמרים בגיט** — `data/` אינו מגובה.

חלוקת העבודה: **Claude מחפש בדרייב** דרך המחבר ומייצר manifest של
`{id, name, modifiedTime}`, ו**הכלי מוריד** דרך חלון הסוכן כדי שהבייטים לא
יעברו בשיחה. מה שכבר סונכרן נרשם ב-`.state.json`, והרצה חוזרת מורידה רק מה
שהשתנה. בסוף הוא מדפיס כמה תיאורי צבע יש ומה עדיין חסר.

### הקמת המאסטר — `color-create` · `model-create`

```bash
npm run run -- color-create --json '{"colors":["206"]}' --confirm   # a125
npm run run -- model-create --json-file runs/models-todo.json --confirm  # a77
```

בקומקס `דגם` ו`צבע` הם ישויות נפרדות שחייבות להתקיים לפני היבוא. שתי המשימות
בודקות כפילות בתיבת החיפוש, מאמתות כל רשומה מיד אחרי השמירה, ורושמות ליומן
`content/מאסטר-שהוקם.json` **רק מה שאומת** — היומן מגשר עד ייצוא הפריטים הבא,
שרק בו הערך החדש יופיע.

⚠️ שני דברים שאי אפשר לנחש: **`#OKNew` שומר בצבעים ולא בדגמים**, ו**שם הדגם
נחתך ל-25 תווים** למרות `maxlength=80`. הפרטים ב-`MAP.md`.

✅ הורץ חי 07/09/2026: 2 צבעים ו-14 דגמים, ושער המאסטר עובר — `0/175`.

הרקע, הנוסחאות ומצב העבודה: [`knowledge/items-setup.md`](knowledge/items-setup.md).
**מסך היבוא של הפריטים עצמם עוד לא ממופה** — הקובץ מוכן, הקליטה עוד לא רצה.

### תעודת העברה → חשבונית

```
node tools/run.js transfer-to-invoice --json '{"transfer":"4700241","customer":"112447"}'
```

קורא תעודת העברה קיימת ומעתיק את שורותיה לחשבונית מס סיטונאית לאותו לקוח.
**מספר התעודה הוא חובה** — לתעודת העברה אין שדה לקוח בכלל (הכותרת שלה היא שני
מחסנים), ולכן אין דרך לחפש "התעודה של לקוח X".

שלושה שלבים, ו-`--confirm` **לא** קולט:

| | מה קורה |
|---|---|
| בלי דגל | קורא את התעודה ומציג את הפריטים. לא נפתחת חשבונית |
| `--confirm` | + פותח חשבונית וממלא כל שורה, ועוצר לפני קליטה |
| `--confirm` + `"file":true` | + לוחץ קליטה |

`file` נפרד מ-`--confirm` בכוונה: קליטת חשבונית מזיזה מלאי ואין לה ביטול.

### מחיר סיטונאי ומע"מ

`wholesale: true` ב-`quote-add-line` מכוון ל**נטו** `round(ברוטו/2)`. דרך ההזנה
נקבעת לפי המחירון של המסמך, שקומקס מצהירה עליו בפוטר
(`לפי מחירון: מכירה ראשי (כולל מע''מ)`):

| | מחירון לפני מע"מ | מחירון כולל מע"מ |
|---|---|---|
| מוזן | מחיר `round(ברוטו/2)`, הנחה 0 | המחיר נשאר, הנחה `(1 − יעד×(1+מע"מ)/ברוטו)×100` |
| שורה | 145.00 | 171.10 |
| **סה"כ** | **171.00** | **171.00** |

האחוז נגזר מהיעד המעוגל — 40.98% ל-289.90 ב-18%, לא 41% עגול. המשימה **עוצרת**
כשהיא לא יודעת אם המחירון כולל מע"מ, וקוראת את הנטו בחזרה מהשדות כדי לוודא
שנחת על היעד לפני שהשורה נשמרת.

**שים לב:** הצעה שלא הופקה נתפסת מחדש — `quote-new` על אותו לקוח יחזור אליה
ויחליף לה את הכותרת. מפיקים (`quote-finalize`) לפני שמתחילים מסמך נוסף.

## גיבוי הזיכרון של הסוכן — `memory/`

```bash
npm run memory-backup              # מקור ⇒ ריפו. חלק מ-push-comax
npm run memory-backup -- --restore # ריפו ⇒ מקור, רק קבצים שחסרים
```

הזיכרון שהסוכן צובר יושב **מחוץ לריפו**, ב-
`~/.claude/projects/C--AGENT-COMAX-CLOAD/memory/`. נמדד 08/09/2026:
`git ls-files` החזיר עליו **0** — 45 קבצים לא היו מגובים בשום מקום, והפרויקט
עובד משני מחשבים.

**הכיוון הוא הכלל:** המקור הוא הסמכות, `memory/` שבריפו הוא גיבוי.

| התנהגות | למה |
|---|---|
| הגיבוי **לא כותב למקור** | ברירת המחדל היא כיוון אחד |
| הגיבוי **לא מוחק מהריפו** | שני מחשבים שמגבים לאותה תיקייה היו מוחקים זה את זכרונות זה. זו מיזוג, לא מראה |
| `--restore` **מוסיף רק חסרים** | לא דורס למידה מקומית במחשב חדש |

⚠️ המחיר: זיכרון שנמחק במקור **נשאר** בריפו. מחיקה אמיתית היא פעולה ידנית
ומודעת — וזה עדיף על מחיקה שקטה בין שני מחשבים.

## `npm run payroll` — דוח הנוכחות החודשי לשכר

```bash
npm run payroll -- --to dror.arena@gmail.com                 # החודש הקודם
npm run payroll -- --to dror.arena@gmail.com --month 08/2026
npm run payroll -- --to dror.arena@gmail.com --dry           # ממלא ועוצר
```

מריץ את `a162`, מאמת שהחודש שנקלט הוא זה שביקשנו, קורא את הדוח, שומר PDF
ל-`runs/downloads/`, ושולח אותו **לדרור עצמו** מתוך קומקס. לוקח את `runs/.lock`,
מתנתק בסוף וסוגר את החלון.

`--to` **חובה, בלי ברירת מחדל** — כלל 14.

**משימה מקומית ב-Task Scheduler:** `COMAX-Payroll-Monthly`, ה-2 בכל חודש ב-08:07,
`StartWhenAvailable=true` כדי שמחשב שהיה כבוי יריץ בהזדמנות הראשונה. הלוג מצטבר
ב-`runs/payroll-cron.log`. היא רצה רק כשיש סשן משתמש — לכרום צריך שולחן עבודה.

⚠️ **המשימה שולחת לדרור בלבד.** ההעברה לרואת החשבון נשארת ידנית ומאחורי המילה
"שלח" — ההערות פר-עובד משתנות כל חודש, וכלי ההעברה בג'ימייל שולח מיד בלי טיוטה.

הפרטים המלאים: [knowledge/payroll-attendance.md](knowledge/payroll-attendance.md)

## חשבונית גדולה — שורות מאקסל במקום אחת-אחת

הזנת שורה בקצב אנושי נמדדה ב-**~36 שניות**. חשבונית וויקס של 188 שורות הייתה
נמשכת כשעתיים עם המושב תפוס. `invoice-import-lines` עושה את זה בדקה וחצי.

```bash
npm run run -- invoice-import-lines --json '{"customer":"112474","store":"WIX","priceList":"מכירה ראשי","details":"...","file":"data/exports/....xlsx"}' --confirm
npm run run -- invoice-finalize     --json '{"customer":"112474","expectDoc":"6500089","expectTotal":"22497","store":"WIX","priceList":"מכירה ראשי"}' --confirm
npm run run -- invoice-reset        --json '{"customer":"112001"}' --confirm
```

**הקובץ:** שלוש עמודות — `ברקוד · כמות · סכום`. שורת כותרת אופציונלית.
`סכום` הוא **סה"כ השורה**, וקומקס גוזר ממנו `מחיר = סכום ÷ כמות`.

⚠️ **המחירון בכותרת קובע מה המספרים בקובץ אומרים.** תחת `מכירה ראשי` הם
נבלעים ככוללי מע"מ — וזה הנכון לוויקס, שהדוח שלהם כולל מע"מ. תחת `מחירון
קבוצות` אותו קובץ ייתן סכום גבוה ב-18%.

**השערים:** `invoice-import-lines` בוחרת את המבנה `ברקוד/קוד-כמות-סכום`
במפורש (ברירת המחדל בקומקס היא בלי סכום ותמשוך מחירים מהמחירון), מוודאת
ש-`תקין + לא תקין` מסתכם למספר השורות בקובץ, ו**עוצרת על כל דחייה**. היא
**אינה קולטת** — הקליטה היא `invoice-finalize` בלבד, עם אימות המספר והסכום
שאושרו.

**שער הפריטים — חובה לפני כל יבוא.** כל ברקוד חייב להיות מוקם בקומקס:
הצלבה מול `data/exports/פריטים-מלא-*.csv` ⇒ אם יש חסרים, `items-export` טרי
והצלבה שוב ⇒ **אימות מול קומקס** בהרצה בלי `--confirm` (בונה תצוגה מקדימה,
לא קולטת) ⇒ הצעת הקמה ⇒ ורק אז יבוא.
⚠️ ההצלבה המקומית לבדה מתריעה שווא: `items-export` מחזיר ברקוד אחד לפריט,
ופריט עם **ברקודים נוספים** ייראה חסר. נמדד: מקומית 7, בקומקס 6.
⚠️ ו-`סה"כ רשומות` בתצוגה המקדימה הוא **מונה עמוד (30)**, לא סכום — ההוכחה
לשלמות היא סכום המסמך מול סכום הקובץ.

⚠️ **טיוטה נתפסת מחדש עם שורותיה**, ויבוא נוסף מוסיף עליהן. מסמך לא ריק
הוא עצירה; `resetExisting` מאפס תחילה, `invoice-reset` מנקה בנפרד.

# פרויקט COMAX AGENT — הוראות עבודה

סוכן שמפעיל את פורטל קומקס בדפדפן אמיתי על המחשב, בקצב אנושי, בלי API.

פרטים טכניים: [README.md](README.md) · מפת המערכת: [knowledge/MAP.md](knowledge/MAP.md)

---

## מפת הסוכנים — מי אחראי על מה

**הסשן הראשי הוא הסוכן-על.** הוא קורא את הטבלה הזאת ומנתב. אל תעשה בעצמך מה
ששייך למומחה — הידע יושב אצלו, לא כאן.

### מסמכים

| סוכן | מסמך | תוכנית | מזיז מלאי | מצב |
|---|---|---|---|---|
| `comax-quote` | הצעת מחיר | `a164` / Doc612 | לא | ✅ מאומת בהרצה חיה |
| `comax-invoice` | חשבונית מס | `a157` / Doc650 | **כן, בלתי הפיך** | ✅ מאומת (6500084) |
| `comax-transfer` | תעודת העברה | `a111` / Doc470 | **כן, שני כיוונים** | ✅ מאומת (4700239) |
| `comax-invoice-receipt` | חשבונית מס/קבלה | `a132` / Doc652 | כן + **גובה תשלום** | ⚠️ מסכים מופו (03/09) · הקליטה לא הורצה — היצירה מסורבת |
| `comax-receipt` | קבלה | `a103` / **אין Doc** — `Kupa/Kab/…` | לא — **רושם כסף וסוגר חשבוניות** | ⚠️ הזרימה אומתה חי (6800005, 03/09) אבל דרור הקליק — `driven: false`, היצירה מסורבת |
| `comax-osh-receipt` | קבלה לעו"ש | `a146` / Doc681 — **`Max2000_NET_2022`** | לא — רושם העברה בנקאית שנכנסה | ✅ **הקוד רשם בעצמו** (6810058, 03/09) · בודק כפילות לפני יצירה |

### קריאה ותשתית

| סוכן | תחום | נוגע בקומקס |
|---|---|---|
| `comax-stock` | "כמה יש", "מה המק\"ט", מלאי לפי מחסן — מ-`content/` | **לא** |
| `comax-history` | "מה הלקוח קנה ובאיזה מחיר" — `a224`, `a157`, `a132`, `a164` | כן, קריאה בלבד |
| `comax-ops` | המושב היחיד · רענון הייצוא · מיפוי מסכים חדשים | כן |
| `gmail` | תכתובת Gmail כללית — חיפוש, קריאה, טיוטה+אישור+שליחה (שתי הרצות), תיוג וארכוב | לא |

### ניתוב מהיר

| הבקשה | הסוכן |
|---|---|
| "תוציא חשבונית ל..." · "לקלוט חשבונית" · "שיחזור" | `comax-invoice` |
| "תכין הצעת מחיר" · "כמה זה יוצא סיטונאי" · "תפיק PDF" | `comax-quote` |
| "תעביר 6 יחידות מראשי לקבוצות" · "תאזן את החשבונית" | `comax-transfer` |
| "תוציא חשבונית עם תשלום" | `comax-invoice-receipt` (יסרב ויסביר) |
| "תרשום קבלה" · "הלקוח שילם במזומן/שיק/אשראי" | `comax-receipt` (יסרב ויסביר) |
| "הלקוח העביר לי כסף" · "קבלה לעו"ש" · "נכנסה העברה בנקאית" | `comax-osh-receipt` |
| "כמה יש מזה" · "איפה יושב הפריט" · "מה במכולה" | `comax-stock` |
| "מה הוא שילם בפעם הקודמת" · "מה קנה השנה" | `comax-history` |
| "המלאי לא מעודכן" · "קוד משתמש בשימוש" · "תמפה מסך" | `comax-ops` |
| חשבונית **ואז** תעודת העברה, כפעולה אחת | שרשור דרך `registry.chain` |
| "תבדוק לי מיילים על..." · "תמצא את ההתכתבות עם..." · "תענה למייל..." · "תשלח מייל ל..." · "תתייג/תארכב" | `gmail` |

**סוכן-העל בקוד** הוא [`src/documents/registry.js`](src/documents/registry.js) —
הוא מנתב בין סוכני המסמכים ברמת ה-JS, בודק מוכנות לפני הקליק הראשון, ומשרשר.
זו שכבה אחרת מהטבלה כאן, ושתיהן נחוצות.

**"שלח במייל" הוא לא תמיד `gmail`.** שליחת הצעת מחיר/חשבונית ללקוח **במייל**
נשארת אצל `comax-quote`/`comax-invoice` — הם שולחים PDF מתוך פורטל קומקס עצמו,
מנגנון נפרד לגמרי מ-Gmail. `gmail` הוא לתכתובת חיצונית כללית, לא למסמכי לקוח.

---

## המוסכמות שלי

| מה שאני אומר | מה שרץ |
|---|---|
| `push-comax` · "סיימתי תגבה" | `git add . && git commit -m "<עדכון>" && git push` |
| `pull-comax` · "תמשוך גיבוי" | `git pull` |

הפרויקט עובד משני מחשבים ומסונכרן דרך
https://github.com/drorarena-commits/comax--agent.git

**`.env` לא נשמר ב-git** (מכיל מפתחות וסיסמאות) — יש לעדכן ידנית בכל מחשב בנפרד.
התבנית היא [`.env.example`](.env.example): `COMAX_ORG` · `COMAX_USER` · `COMAX_PASS`.

גם `data/`, `runs/`, `node_modules/` ו-`.chrome-profile/` לא נשמרים.
**`content/` כן נשמר** — שם יושב הייצוא המקומי שכל תשובות המלאי מגיעות ממנו.

**קידוד: UTF-8 בלבד.** קובץ עברית ב-CP1255 מוצג כג'יבריש בטרמינל ובעריכה.

---

## כללים שחוצים את כל הסוכנים

1. **מושב אחד — נאכף בקוד, לא כאן.** לקוד המשתמש בקומקס יש מושב יחיד, ושתי
   פעולות שרצות במקביל מפילות אחת את השנייה. שני המנגנונים:

   **שחרור המושב הוא יזום — קומקס לא ישחרר לבד.** אין יותר `logoff` בסוף כל
   משימה: הסשן נשאר פתוח כדי שבקשות רצופות מהאייפון ירוצו מיד בלי לוגין חוזר.
   אבל **חלון פתוח משאיר את הסשן חי בעצמו** — הפריים-סט מתשאל את השרת כל ~90
   שניות (נמדד; ממצא 3 ב-`MAP.md`), ולכן הניתוק האוטומטי לא יקרה והקוד יישאר
   תפוס כל היום. לכן [`tools/idle-logoff.js`](tools/idle-logoff.js) משחרר אחרי
   **10 דקות רצופות** בלי שימוש, והשעון **מתחיל מחדש עם כל שימוש**
   ([`src/activity.js`](src/activity.js)) כדי שלא ינותק באמצע עבודה.
   `npm run logoff` נשאר לשחרור מיידי.

   ⛔ **אסור להוסיף ניטור תקופתי שנחשב "שימוש".** השחרור נשען על
   `runs/.last-activity`, לא על טיימר של קומקס. כל בדיקה תקופתית שקוראת ל-
   `touch()` או מריצה משימה על טיימר תדחוף את החותמת קדימה לנצח, `idle-logoff`
   לא יתנתק אף פעם, והמושב יישאר תפוס — בלי שגיאה ובלי סימן.

   ⛔ **ואי אפשר לזהות סשן מת ב-GET.** נמדדו 37 כתובות, 0 שונות בין סשן חי לסשן
   שנהרג (ממצא 4 ב-`MAP.md`). `isLoggedIn()` הוא DOM בלבד וימשיך לשקר. לכן
   ההתאוששות יושבת בנקודת הכישלון ב-[`tools/run.js`](tools/run.js): קריאה —
   לוגין מחדש וניסיון שני; **כתיבה — לוגין מחדש, עצירה ודיווח**, כי טיוטה שמתה
   עם הסשן תיצור מסמך חלקי או כפול אם "ממשיכים מאותה נקודה".

   **"פעולה אחת בכל רגע" נאכפת ב-[`src/lock.js`](src/lock.js)** — `runs/.lock`
   נלקח בתחילת כל הרצה (וגם ב-`logoff`) ומשוחרר בסופה; הרצה שנייה מסרבת ואומרת
   מי מחזיק. **בקוד, לא כהוראה כאן** — כמו כללים 11 ו-13, כי מאז שהסוכן זמין
   מהאייפון אני כבר לא היחיד שיכול להתחיל הרצה.

   `npm run open` נשאר למיפוי ולעבודה ידנית מול החלון.

2. **בלי `--confirm` קודם.** כל משימה כותבת ממלאת את הטופס, מצלמת, ועוצרת לפני
   כפתור השמירה. משימות קריאה בלבד מסומנות `writes: false` ורצות חופשי.

3. **להראות סכומים, מחירים לשורה ומשטר המחירון לפני שמבקשים אישור.** `--confirm`
   רק אחרי אישור מפורש שלי.

4. **`#OK` במסך שורות הוא "קליטת מסמך".** במסך כותרת אותו `#OK` רק מתקדם. היציאה
   בלי לקלוט היא `#DoExit` ואז `#Cancel` — אף פעם `#OK`.

5. **פריטים לפי מק"ט ברקוד רגיל בהזנה לקומקס, ולפי מק"ט חלופי / דגם+צבע בתצוגה
   לדרור.** לעולם לא ברקוד על המסך, ולעולם לא `AR...` לתוך `#Prt`.

6. **עותקים: הצעה = 0 · תעודת העברה = 0 · חשבונית מס = 1.** אל תעתיק ערכים בין
   המסמכים — 0 בחשבונית נדחה בשקט, וערך ≠ 0 בהצעה פותח את דיאלוג ההדפסה של
   Chrome, שחוסם את הדפדפן לחלוטין עד שאדם מנקה אותו.

7. **מלאי נענה מ-`content/`, לא מדוח חדש.** דוח מטריצה = 2.5 דקות, ייצוא מלא =
   כחצי שעה, ושניהם תופסים את המושב. `comax-ops` מריץ, ורק אחרי שאישרתי.

8. **חוסר בקובץ הוא לא אפס** — הוא "≤0 במחסנים שהדוח כיסה". הייצוא מכסה ראשי ·
   רמת גן · מכולה וינגייט · WIX · ספורט & מור רמלה. **מחסן קבוצות (11) לא בפנים.**

9. **"לא ידוע" הוא סירוב, לא ניחוש.** משטר מע"מ לא ידוע, מלאי מקור לא מכוסה, מסך
   לא ממופה — עוצרים ואומרים. הניחוש הזה עלה 15% תת-גבייה בהצעה 6120045.

10. **לסגור חלונות בסוף** (`npm run close -- all`) — חלון פתוח בולע את הדאבל-קליק
    של המשימה הבאה.

11. **קבלה: לבדוק כפילות לפני שרושמים.** לפני כל קבלה או קבלה לעו"ש — להציץ ב-5
    הקבלות האחרונות של אותו לקוח. **אם כבר יש אחת על אותו סכום: לעצור, להתריע,
    ולחכות לאישור שלי.** בלי אישור לא נוצרת קבלה. אותו תאריך בלבד הוא הערה, לא
    חסימה. ממומש ב-[`src/documents/duplicate-check.js`](src/documents/duplicate-check.js)
    ונקרא מ-`create()` — **בקוד, לא כהוראה כאן**, כי כלל שכתוב רק במסמך נשכח
    בדיוק בפעם שהוא חשוב.
    ⚠️ הבדיקה מנקה את כל תיבות הסינון קודם: הן מצטברות, וסינון ישן שנשאר גורם
    ל"אין לו קבלות אחרות" בשקט.

12. **ממצא חדש מהרצה חיה נכתב ל-`MAP.md` ולסוכן הרלוונטי, באותו commit.** מה שלא
    מתועד — נגלה מחדש, בדרך כלל על מסמך אמיתי.

13. **קבלה: ללקוח חייבות להיות חשבוניות מס לפני שרושמים.** לפני כל קבלה או
    קבלה לעו"ש — לבדוק ברשימת חשבוניות מס (a157) שלקוח **הקוד הזה** קיבל אי
    פעם חשבונית. **לא בודקים אם היא פתוחה או סגורה** — רק שיש בכלל. אם אין:
    לעצור ולשאול, לא לנחש. זו הגנה מפני קבלה שנרשמת ללקוח הלא נכון מתוך שני
    לקוחות בעלי שם דומה — קבלה בדרך כלל באה בעקבות חשבונית, ולקוח בלי אף
    חשבונית הוא סימן שנבחר הקוד הלא נכון. ממומש ב-
    [`src/documents/invoice-presence-check.js`](src/documents/invoice-presence-check.js)
    ונקרא מ-`create()` **בקוד**, כמו כלל 11. דגל `allowNoInvoice: true` הוא
    האישור המפורש שלי לקבל לקוח בלי חשבוניות (לקוח חדש, למשל).

14. **שליחת מסמך דורשת נמען מפורש — לעולם לא מה שקומקס מילא.** מסך המעטפה
    (`Erp/Divor_Doc.asp`) נפתח **טעון בכתובת של הלקוח**, מכרטיס הלקוח, בכל סוגי
    המסמכים. זו לא תקלה חד-פעמית אלא ההתנהגות הקבועה: נצפה `erez@kmc.co.il`
    בהצעת מחיר, ו-`felixrubin88@gmail.com` בחשבונית 6500085 שביקשתי **לעצמי**.
    המשמעות: כל בקשה שלי מהטלפון מתחילה טעונה בכתובת של הלקוח, וקליק אחד שולח
    מסמך חי ללקוח אמיתי — בלתי הפיך, בזמן שאף אחד לא ליד המסך.

    אין נמען מפורש → **עצירה, לא ניחוש**. ממומש ב-
    [`src/documents/recipient.js`](src/documents/recipient.js) **בקוד**, כמו
    כללים 11 ו-13, ונקרא מכל זרימת שליחה. שלושה שלבים, וכולם נדרשים:
    `requireRecipient` לפני שנוגעים בקומקס · `takeOverRecipient` שמנקה את השדה
    ב-`fill('')` לפני ההקלדה — הוספה לתוכן קיים הייתה מייצרת שדה עם **שתי**
    כתובות, והמסמך היה מגיע גם ללקוח · `assertRecipient` לפני הקליק, בהשוואת
    **שוויון מדויק** ולא הכלה, כי שדה עם שתי כתובות "מכיל" את הנכונה ויעבור בשקט.

---

## איפה הידע יושב

```
CLAUDE.md                        מפת הסוכנים והמוסכמות (הקובץ הזה)
.claude/agents/*.md              סוכני Claude — מה כל מומחה יודע ומתי לא הוא
src/documents/AGENT.md           סוכן-העל בקוד — registry.js
src/documents/agents/*/AGENT.md  המלכודות של כל מסמך, ומצב המיפוי שלו
knowledge/MAP.md                 מפת קומקס — ממצאים מהרצות חיות
knowledge/contacts.md            מי-זה-מי — צוות טכני + ספקים ושותפים עסקיים
knowledge/screens/               מתכון הסלקטורים לכל מסך שמופה
config/comax.config.json         כתובות, קצב אנושי, timeouts, מחסני הדוח
content/                         הייצוא המקומי — מלאי ולקוחות
src/tasks/                       משימה אחת = קובץ אחד
```

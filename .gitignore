node_modules/
.chrome-profile/
runs/
.env
data/

# ספורט אנד מור — קבצי עבודה. הייחוס ב-sportmore/reference/ כן נשמר.
sportmore/in/
sportmore/out/

# כרטיסי הפריט מתגלגלים — כל מנה מביאה חדש, וכל אחד הוא כ-11MB. הם נשארים
# מקומיים, **חוץ מהכרטיס המוצהר** שהרגרסיה נעולה עליו: בלעדיו npm run sm-test
# לא יכול לרוץ על המחשב השני, ובדיקה שתלויה בקובץ שאינו בגיט אינה בדיקה.
sportmore/reference/item-card-*.xlsx
!sportmore/reference/item-card-2026-08-25.xlsx

# רישום האירוע שמיישן את כרטיס הפריט — **נשמר בגיט במכוון**. הוא אינו קובץ
# עבודה אלא רישום של אירוע עסקי, וכל מודל העדכניות תלוי בו: בלעדיו מחשב אחר
# היה אומר "אין קובץ הקמה" ונותן את התשובה ההפוכה. החריגה כאן מפורשת כדי שכלל
# רחב שיתווסף בעתיד (למשל *.json) לא יבלע אותו בשקט.
!sportmore/reference/last-setup.json
.env.bak-*

# גשר הוואטסאפ — סשן חי, שווה ערך לסיסמה. לא נשמר בגיט בשום מצב.
.whatsapp-profile/

# מטמון של whatsapp-web.js — נוצר בזמן ריצה
.wwebjs_cache/

# תור המשימות — תוכן זמני בזמן ריצה. SPEC.md כן נשמר.
queue/inbox/
queue/working/
queue/hold/
queue/approve/
queue/done/

# תוצרי עבודה מקומיים שנוצרו בשיחה — לא ידע פרויקט
content/Claude outputs/
